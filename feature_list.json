{
  "project": "concord-notion-conflict-dashboard",
  "last_updated": "2026-07-09",
  "rules": {
    "single_active_feature": true,
    "passing_requires_evidence": true,
    "do_not_skip_verification": true
  },
  "status_legend": {
    "not_started": "Work has not begun.",
    "in_progress": "The feature is the current active task.",
    "blocked": "Work cannot continue until a documented blocker is resolved.",
    "passing": "Required verification has passed and evidence is recorded."
  },
  "features": [
    {
      "id": "auth-001",
      "priority": 1,
      "area": "auth",
      "title": "Notion OAuth login redirect",
      "user_visible_behavior": "Clicking 'Continue with Notion' sends the user to Notion's authorize screen.",
      "status": "passing",
      "verification": [
        "Start the server: cd server && npm start.",
        "curl -s -o /dev/null -w '%{http_code} %{redirect_url}\\n' http://localhost:3001/auth/",
        "Assert HTTP 302 and the redirect URL points to api.notion.com/v1/oauth/authorize with the configured client_id and redirect_uri."
      ],
      "evidence": [
        "server/api/auth.ts router.get('/') builds the authorize URL and calls res.redirect.",
        "Covered by the ./init.sh baseline smoke check."
      ],
      "notes": "Requires CLIENT_ID in .env."
    },
    {
      "id": "auth-002",
      "priority": 2,
      "area": "auth",
      "title": "OAuth callback upserts the user and redirects to /Dashboard",
      "user_visible_behavior": "After authorizing on Notion, the user is returned, their profile is stored, and they land on /Dashboard.",
      "status": "passing",
      "verification": [
        "Complete the Notion OAuth flow against http://localhost:3001/auth/callback with a valid code.",
        "Verify a User row is upserted in Postgres (email, name, notionId, workspaceId, avatar).",
        "Verify the browser is redirected to http://localhost:3000/Dashboard exactly once (no double-send)."
      ],
      "evidence": [
        "Fixed double-send in server/api/auth.ts success branch: now `return res.redirect('http://localhost:3000/Dashboard')` only (removed the trailing res.json).",
        "Error paths verified 2026-07-01: GET /auth/callback (no code) -> HTTP 400; GET /auth/callback?code=bogus -> HTTP 400 (Notion rejects), 0 occurrences of ERR_HTTP_HEADERS_SENT in server log.",
        "Baseline smoke check still passes after the change.",
        "Manual OAuth run verified 2026-07-01: real Notion login redirects once to /Dashboard (404 there is expected until dash-001) and the User row was upserted correctly in Postgres."
      ],
      "notes": "Passing. Success path confirmed by a manual Notion OAuth run (single 302 to /Dashboard + User upsert). The /Dashboard 404 is tracked separately by dash-001. (.env key is CLIENT_SECERT and the code matches it - left as-is on purpose.)"
    },
    {
      "id": "dash-001",
      "priority": 3,
      "area": "dashboard",
      "title": "/Dashboard route renders after login",
      "user_visible_behavior": "An authenticated user sees the dashboard shell at /Dashboard instead of a 404.",
      "status": "passing",
      "verification": [
        "Start the frontend: npm run dev.",
        "curl -s -o /dev/null -w '%{http_code}\\n' http://localhost:3000/Dashboard",
        "Assert HTTP 200 and the dashboard shell renders."
      ],
      "evidence": [
        "Added app/Dashboard/page.tsx: a server component (no data fetching) rendering the dashboard shell (header + placeholder region). Root app/layout.tsx wraps it.",
        "Redirect target confirmed from source: server/api/auth.ts:81 returns res.redirect('http://localhost:3000/Dashboard') (capital-D), matching the app/Dashboard/ folder.",
        "Verified 2026-07-01: ./init.sh baseline smoke check still PASSES (/auth/ -> 302, / -> 200).",
        "Verified 2026-07-01: npm run dev, then curl http://localhost:3000/Dashboard -> HTTP 200 (was 404); response body contains the shell copy 'Edit conflicts detected across your Notion workspace.' Dev server torn down (port 3000 -> 000), clean state."
      ],
      "notes": "Passing. Shell only (200 render) — listing conflicts is tracked by conflict-002, which will extend app/Dashboard/page.tsx once conflict-001 lands. No auth gate added on purpose (the verification curls /Dashboard with no session and expects 200)."
    },
    {
      "id": "auth-003",
      "priority": 4,
      "area": "auth",
      "title": "Persist the Notion access token on the User during OAuth callback",
      "user_visible_behavior": "After a user authorizes on Notion, their access token is saved so the app can later read their workspace on their behalf.",
      "status": "passing",
      "verification": [
        "Add an accessToken field to the User model in prisma/schema.prisma and apply it to the DB (prisma migrate/db push), then npx prisma generate.",
        "Update the prisma.user.upsert in server/api/auth.ts to store response.access_token on both create and update.",
        "Complete an OAuth login (or exercise the upsert path) and verify the User row for that notionId has a non-null accessToken in Postgres.",
        "Confirm ./init.sh baseline smoke check still passes and the token value is never written to logs or committed files."
      ],
      "evidence": [
        "Schema: added `accessToken String?` to the User model in prisma/schema.prisma (nullable, additive).",
        "Migration drift resolved without data loss: the pre-existing `avatar` column was in the DB but not in any migration (would have forced `prisma migrate dev` to reset). Baselined it via migration 20260701120000_baseline_avatar marked `--applied` (recorded, not executed), then applied 20260701120100_add_user_access_token via `prisma migrate deploy`. `prisma migrate status` = up to date.",
        "DB verified 2026-07-01: User table columns now include `accessToken`; the 1 existing User row survived (no data loss). Existing row has NULL accessToken (expected until the next login).",
        "Code: server/api/auth.ts upsert now sets `accessToken: AccessToken` in both create and update; the success console.log still omits the token (not logged).",
        "Baseline: ./init.sh smoke check PASSES after the change (client regenerated with the new field; /auth/ -> 302, / -> 200). NOTE: baseline does not exercise the callback success path, so it is supporting evidence only.",
        "Primary evidence confirmed 2026-07-01: maintainer performed a manual Notion OAuth login and confirmed the access token is now saved on the User (non-null accessToken persisted via the callback upsert)."
      ],
      "notes": "Passing. Column added, data intact, upsert writes response.access_token on create and update, baseline passes, and a manual OAuth login confirmed the token is saved. Token stored plaintext (MVP; encryption-at-rest is out of scope, noted for future hardening). Treated as a secret: never logged. sync-001 can now use this per-user token."
    },
    {
      "id": "sync-001",
      "priority": 5,
      "area": "sync",
      "title": "Fetch and snapshot Notion pages/blocks per workspace",
      "user_visible_behavior": "After connecting, the user's Notion pages and block snapshots are stored for the workspace.",
      "status": "passing",
      "verification": [
        "Trigger a sync for a connected workspace.",
        "Verify Page rows exist for the workspace (notionPageId, tittle, workspaceId).",
        "Verify Snapshot rows are created linking blockId, pageId, and userId."
      ],
      "evidence": [
        "Added the reusable sync routine server/lib/sync.ts: syncWorkspaceForUser(user) creates an authed Notion client from user.accessToken (auth-003), searches pages ({ property: 'object', value: 'page' }), upserts a Page per page on the stable notionPageId (tittle from the title-type property, fallback 'Untitled'; workspaceId from the user), then lists each page's top-level blocks and creates a Snapshot { blockId, pageId, userId } per block. Never logs the token; returns { pages, snapshots } counts.",
        "The per-page upsert+snapshot block is wrapped in try/catch and continues on error so a duplicate `tittle` (Page.tittle is @unique) can't abort the whole run.",
        "Added a thin trigger server/scripts/runSync.ts + server package.json script 'sync' (tsx --env-file=../.env scripts/runSync.ts). It resolves the first User with a non-null accessToken and calls the routine. The HTTP endpoint (sync-004 POST /sync) and 60s interval (sync-002) will reuse the same routine.",
        "Baseline verified 2026-07-01: ./init.sh smoke check still PASSES (/auth/ -> 302, / -> 200); npx prisma generate OK. No schema change, no new dependency.",
        "Live sync verified 2026-07-01: `npm run sync` against the connected workspace ddb7f940… (user id 1) printed 'Sync complete: 4 page(s) upserted, 86 snapshot(s) created' (token not logged).",
        "DB assertions confirmed 2026-07-01: 4 Page rows with notionPageId, tittle ('Conflict Dasboard Flow', 'Accenture', 'Next js', 'Code Flow'), and workspaceId populated; 86 Snapshot rows linking blockId, pageId, and userId (all userId=1)."
      ],
      "notes": "Passing. Prisma models Page and Snapshot already existed; no migration. MVP limitations (deferred, not built): no pagination (first ~100 pages / ~100 blocks per page), no recursion into nested block children, no block content stored (Snapshot has no content column — conflict-001 owns adding it), single-user token drives a multi-user workspace sync. Snapshots are append-only (one row per block per run), giving sync-002 a time series."
    },
    {
      "id": "conflict-001",
      "priority": 6,
      "area": "conflict",
      "title": "Detect concurrent edits and record Conflict rows",
      "user_visible_behavior": "When two users edit the same block around the same time, a conflict is recorded.",
      "status": "passing",
      "verification": [
        "Create two snapshots for the same blockId by two different users.",
        "Run conflict detection.",
        "Verify a Conflict row is created with pageId, blockId, user1Id, user2Id, and status='unresolved'."
      ],
      "evidence": [
        "Added server/lib/conflict.ts exporting detectConflicts(): groups all Snapshot rows by blockId, keeps each distinct userId's latest snapshot on that block, and for blocks with 2+ distinct contributors takes the two earliest-by-latest-touch as user1/user2. Dedupes against any existing Conflict for that blockId + user pair (either order) before creating, so re-runs don't spam duplicates. Creates with status='unresolved', resolvedBy='' (placeholder — Conflict.resolvedBy is non-null with no default; no migration needed).",
        "Added server/scripts/runDetectConflicts.ts (thin trigger, mirrors runSync.ts) + server/package.json script 'detect-conflicts' (tsx --env-file=../.env scripts/runDetectConflicts.ts). A future sync-002 interval or endpoint can call detectConflicts() directly.",
        "Verification status value changed from 'open' (as originally written here) to 'unresolved' to match conflict-003 (GET /conflicts filters status='unresolved'), conflict-004 (unresolved->resolved transition), conflict-005, and ALL_PHASE.md — 4 other entries vs. this one, and 'open' would have made detected conflicts invisible to the rest of the pipeline. Flagged for maintainer to confirm on review.",
        "No schema change: the Conflict model already had every column needed (pageId, blockId, status, resolvedBy, user1Id, user2Id).",
        "Verified 2026-07-02: DB had 1 User (id 1) and 129 Snapshot rows (all userId=1), 0 Conflict rows. Seeded a synthetic second User (id 6, email conflict-001-test-user2@example.com, same workspaceId as user 1) and one Snapshot (id 130) on an existing blockId ('3267f940-ff25-80ce-8952-fd1adad9c443')/pageId(1) with userId=6.",
        "Ran `npm run detect-conflicts` (from server/): 'Conflict detection complete: 1 conflict(s) created.' DB query confirmed the row: pageId=1, blockId='3267f940-ff25-80ce-8952-fd1adad9c443', user1Id=1, user2Id=6, status='unresolved', resolvedBy=''.",
        "Re-ran `npm run detect-conflicts`: 'Conflict detection complete: 0 conflict(s) created.' Conflict table count stayed at 1 row — dedup confirmed working.",
        "Baseline: ./init.sh smoke check PASSES after the change (/auth/ -> 302, / -> 200). No schema/migration touched.",
        "Cleanup 2026-07-02: deleted the seeded Conflict (id 1), Snapshot (id 130), and test User (id 6) rows after verification (in FK order) since the test user shared the real workspaceId and would have shown up as a real teammate/conflict in team-001/dash-002/dash-004 later. DB confirmed restored to the pre-verification state: 1 User, 129 Snapshot rows, 0 Conflict rows."
      ],
      "notes": "Passing. Prisma model Conflict already existed (relations ConflictUser1/ConflictUser2). MVP limitations (deferred, not built): Snapshot.userId records who synced a block, not who edited it in Notion (no last_edited_by/last_edited_time captured), so detection is presence-based (2+ distinct syncing users on a block) rather than true concurrent-edit detection; no time window (any two contributors ever seen count, regardless of how far apart); with 3+ contributors on one block only the earliest pair is flagged (Conflict is a two-party model). The seed data used to verify this (test User id 6, Snapshot id 130, Conflict id 1) was deleted after verification rather than left in the DB, since the test user shared the real workspaceId and would otherwise appear as a real teammate/conflict in team-001/dash-002/dash-004. conflict-002/003/004 will need their own seeded Conflict row for their own verification."
    },
    {
      "id": "conflict-002",
      "priority": 7,
      "area": "dashboard",
      "title": "Dashboard lists conflicts with status and resolver",
      "user_visible_behavior": "The dashboard shows detected conflicts with their status and who resolved them.",
      "status": "passing",
      "verification": [
        "Seed at least one Conflict row.",
        "Open http://localhost:3000/Dashboard.",
        "Verify the conflict appears in the list with its status and resolvedBy value."
      ],
      "evidence": [
        "Added app/lib/prisma.ts: a PrismaClient for the Next.js app (server/PrismaClient.ts's adapter pattern was Express-only; the frontend had none). Uses @prisma/adapter-pg + pg.Pool against DATABASE_URL (already root-level deps), cached on globalThis in non-production so Next dev's hot-reload doesn't leak a new pg.Pool per edit. DATABASE_URL needs no extra wiring — Next.js auto-loads .env from the project root.",
        "app/Dashboard/page.tsx is now an async server component: queries prisma.conflict.findMany({ orderBy: { createdAt: 'desc' }, include: { page, user1, user2 } }) and renders one card per conflict — page title (falls back to blockId), a status badge (amber=unresolved/green=resolved), both users, and 'Resolved by: <resolvedBy>' (renders '—' when resolvedBy is '', which is the placeholder conflict-001 writes for still-unresolved rows). Kept the original empty state ('No conflicts to show yet.') when the query returns zero rows. Server component (no 'use client'), so the list renders into the initial HTML — no dependency on GET /conflicts (conflict-003, not yet built).",
        "Confirmed no bundler friction: Prisma's pg driver adapter (wasm query compiler + pg) loaded and queried fine inside the Next 16 Turbopack server-component graph with no config changes — the anticipated serverExternalPackages fallback in next.config.ts was not needed.",
        "Verified 2026-07-02 (first pass, cwd bug caught and redone — see below): DB before seeding — 1 User, 4 Pages, 0 Conflicts. Seeded a synthetic second User (conflict-002-test-user2@example.com, same workspaceId as the real user) and two Conflict rows against a real pageId: one status='resolved' with resolvedBy='conflict-002-test-resolver@example.com' (non-empty, to actually exercise the resolver display) and one status='unresolved' with resolvedBy='' (to exercise the '—' fallback), via a one-off script (not committed, same pattern as conflict-001).",
        "Corrected run 2026-07-02: the first verification pass's `npm run dev` was accidentally invoked with cwd still set to server/ (which has no 'dev' script), so the curl that returned 200 was actually served by a leftover dev-server process, not the command the evidence claimed — caught on self-review before recording. Redid it clean: killed all stray next dev/next-server processes and the .next/dev/lock, re-seeded, ran `npm run dev` from the repo root, and confirmed via the server log itself ('GET /Dashboard 200 in ...') that this run served the request. curl http://localhost:3000/Dashboard -> HTTP 200; response body (grepped) contains both seeded blockIds, both status strings ('resolved'/'unresolved') with the correct badge colors, the resolver email text, the 'Resolved by' label, the em dash fallback, and both users' names.",
        "Cleanup 2026-07-02: deleted both seeded Conflict rows and the synthetic test User via the same one-off script's cleanup counterpart (not committed). DB confirmed restored: 1 User, 4 Pages, 0 Conflicts. Stopped the dev server and confirmed ports 3000/3001 free.",
        "./init.sh baseline smoke check re-run after cleanup: PASSES (/auth/ -> 302, / -> 200, Postgres reachable); npx tsc --noEmit clean. No leftover dev processes/ports after verification (also cleared two stray dev processes left over from an earlier ./init.sh run this session whose EXIT trap hadn't fully torn them down — pre-existing init.sh cleanup gap, not introduced by this feature, not fixed here since out of scope)."
      ],
      "notes": "Passing. Depended on dash-001 and conflict-001 (both passing); did not depend on GET /conflicts (conflict-003, still not_started) per the original note. Scope intentionally excludes stat cards (dash-002), the conflicts-over-time chart (dash-003), member filtering (team-004), and the side-by-side Keep-User1/Keep-User2 view (conflict-005) — those are separate features. MVP limitation carried over from conflict-001: resolvedBy is '' (not null) for unresolved conflicts since Conflict.resolvedBy is a non-null column; the UI treats '' as 'not yet resolved' and shows '—'. UNVERIFIED RISK for later sessions: app/Dashboard/page.tsx does an uncached Prisma read in a server component with no dynamic-rendering marker (e.g. `export const dynamic = 'force-dynamic'`). `next dev` renders per-request so dev/curl verification is legitimately green, but under `next build` (deploy-002) Next may try to statically prerender this route, which could bake in build-time data or fail if the DB isn't reachable at build time. Not fixed here (no build step is exercised by this repo's verification yet, and adding the marker now would be speculative); flag it before deploy-002, and note every later feature that extends this same page (sync-003, dash-002/003, conflict-005) inherits the same risk. Also: init.sh's EXIT trap does not always kill the full process tree it spawns (observed twice this session, unrelated to this feature) — worth a look before relying on it unattended."
    },
    {
      "id": "sync-002",
      "priority": 8,
      "area": "sync",
      "title": "60-second polling snapshot engine",
      "user_visible_behavior": "Once a workspace is connected, its pages and block snapshots refresh automatically about every 60 seconds without any manual action.",
      "status": "passing",
      "verification": [
        "Start the server and connect a workspace so at least one Page row exists.",
        "Confirm a setInterval (~60000ms) re-runs the sync-001 fetch+snapshot routine.",
        "Wait for two intervals and verify new Snapshot rows (fresh createdAt) are appended for the same blocks.",
        "Confirm the interval is set up once at server boot and does not stack duplicate timers on hot reload."
      ],
      "evidence": [
        "Added server/lib/syncScheduler.ts exporting startSyncPolling(intervalMs = 60_000) / stopSyncPolling(). Reuses syncWorkspaceForUser() (sync-001) unchanged. Selects the same 'first user with a non-null accessToken' as runSync.ts (prisma.user.findFirst), not all connected users — deliberate: conflict-001's detection is presence-based (2+ distinct syncing users on a block = Conflict), so auto-syncing every connected user every 60s would flood false conflicts the moment a second real user connects. Interval handle + an isRunning flag are cached on globalThis (same pattern as app/lib/prisma.ts) so a repeat startSyncPolling() call in the same process reuses the existing timer instead of stacking a new one, and an in-flight tick causes the next tick to log-and-skip rather than run concurrently. Token is never logged.",
        "Wired into server/server.js: startSyncPolling() is called once inside the app.listen() callback (server boot); stopSyncPolling() added to the existing SIGINT/SIGTERM handlers alongside server.close().",
        "Verified 2026-07-02: confirmed the stored connected user's Notion accessToken still works and measured real sync duration before designing the timing test — ran the existing 'npm run sync' (DB: 129 -> 215 snapshots, 4 pages, 86 new snapshots) and timed a second manual run at ~21s (mostly Notion API latency across 4 pages' worth of block-listing calls).",
        "Exercised the scheduler live with a short interval via a throwaway script (not committed): called startSyncPolling(5000) twice back-to-back — logged 'Sync polling started: every 5000ms.' exactly once and the second call returned the identical handle ('Same handle returned: true'), confirming within-process idempotency. Note the scope of this check: server/server.js runs today via plain `tsx --env-file=../.env server.js` with no watch/dev mode, so this does not exercise a true hot-reload — the globalThis cache is the same mechanism that would protect a future `tsx watch` setup, matching the app/lib/prisma.ts precedent, but that scenario itself was not exercised.",
        "Let the 5s-interval scheduler run ~50s against the real connected workspace: the live log showed several 'Sync poll complete: ... 4 page(s), 86 snapshot(s)' completions interleaved with 'Sync poll skipped: previous run still in progress.' lines — the overlap guard fired for real (each real sync run, ~21s, comfortably outlasts the 5s tick). The visible-log completion count was an undercount, corrected by a direct DB reconciliation (see next bullet); the properties actually being tested — ticks fire, overlaps get skipped, completions write real rows — are unaffected.",
        "Reconciled exact run count directly from the DB rather than trust the captured log line count: total Snapshot rows went from 215 (after the token-check run) to 697 (net +482, real growth against the real workspace). Rather than back-compute a run count from the total delta (per-run size varies when a page is skipped, so that arithmetic doesn't reconcile cleanly), queried one specific blockId's own row history directly — unambiguous, since each run writes at most one row per block. That block shows 9 total rows: 2 pre-session (2026-07-01 and earlier 2026-07-02) plus exactly 7 new rows this session, timestamped 10:53:49 and 10:54:38 (the two manual runs: token-check + timing calibration) and five more between 10:55:22 and 10:56:01 (five real, non-skipped scheduler ticks during the ~50s live window) — one more completion than the live log appeared to show, confirming the scheduler ran (and the guard correctly let through) more real ticks than were visually counted from the terminal capture. Distinctness check: 86 distinct blockIds total (matches the workspace's real block count) — confirms genuine append-only time-series growth for the same blocks across every run, not new/different blocks appearing. These are real product snapshots against the real connected workspace, not synthetic test data, so they were intentionally left in the DB (not cleaned up, unlike conflict-001/conflict-002's synthetic seed rows).",
        "Confirmed the production default without a live ~130s wait: started the real server (npm start --prefix server) and grepped its boot log for 'Sync polling started: every 60000ms.' — present. The tick logic itself was already proven live above on the same code path at a shorter interval, so this is a faithful substitute for waiting two full default-length intervals.",
        "./init.sh baseline smoke check re-run after all verification: PASSES (/auth/ -> 302, / -> 200); npx tsc --noEmit (root) clean (server/ has no separate tsconfig / typecheck step in this repo's verification path, same as the existing server/lib/conflict.ts and server/lib/sync.ts). No leftover dev processes or listeners on 3000/3001 after verification; all throwaway test scripts deleted."
      ],
      "notes": "Passing. Depends on sync-001 (passing). Design decision worth flagging on review: syncs only the first connected user per tick (matching runSync.ts), not all connected users — see evidence for the conflict-001 false-positive-flood reasoning; revisit once team-001/multi-user workspaces are real and conflict-001 detection gets a time window. detectConflicts() is deliberately not called from this interval (single_active_feature — that integration is conflict-001's note to a *future* feature, not this one). Token kept off logs throughout, per AGENTS.md."
    },
    {
      "id": "sync-003",
      "priority": 9,
      "area": "dashboard",
      "title": "Pages list with last-synced timestamp in the dashboard",
      "user_visible_behavior": "The dashboard lists every connected Notion page and shows when each was last synced.",
      "status": "passing",
      "verification": [
        "Seed or sync at least one Page row (tittle, workspaceId, notionPageId) with related Snapshot rows.",
        "Open http://localhost:3000/Dashboard.",
        "Verify each page appears with its title and a last-synced time derived from the newest related Snapshot.createdAt."
      ],
      "evidence": [
        "app/Dashboard/page.tsx: added a 'Connected pages' section fetching `prisma.page.findMany({ orderBy: { createdAt: 'desc' }, include: { snapshots: { orderBy: { createdAt: 'desc' }, take: 1 } } } )` concurrently with the existing conflicts query via Promise.all; renders one card per page with `page.tittle` and a 'Last synced' line formatted via a fixed en-US/UTC Intl.DateTimeFormat, falling back to 'Never synced' when a page has zero Snapshot rows.",
        "Used real existing data (4 Page rows, 697 Snapshot rows already present from sync-001/sync-002 sessions) rather than seeding synthetic rows -- this feature's verification steps were already satisfiable from live product data.",
        "Captured DB ground truth directly before starting servers: 4 pages (Code Flow, Next js, Accenture, Conflict Dasboard Flow) each with a distinct newest-Snapshot.createdAt in the 2026-07-02T10:56:0x range, all formatting to 'Jul 2, 2026, 10:56 AM' under the chosen formatter.",
        "Booted both dev servers the same way init.sh does (setsid, Express 3001 + Next dev 3000); curl -s http://localhost:3000/Dashboard -> HTTP 200, confirmed served by the run under test via the server's own access log ('GET /Dashboard 200').",
        "Response body checks (occurrence-based grep -o | wc -l, accounting for Next.js dev mode embedding both rendered HTML and an RSC hydration payload in one response -- every string legitimately appears 2x): 'Connected pages' heading present (2 occurrences = 1 real render); 'Last synced' present 8x (4 pages x 2); all 4 real page titles present; the expected formatted timestamp 'Jul 2, 2026, 10:56 AM' present 8x, matching the DB ground truth exactly; 'Never synced' present 0x (correct, since all 4 real pages currently have snapshots); 0 real Conflict rows in DB so the conflicts section correctly rendered its existing empty state ('No conflicts to show yet'). Caveat: since all 4 real pages' newest-Snapshot timestamps collapsed to the same displayed minute, the HTML grep alone cannot distinguish 'each card shows its own page's correct time' from 'every card coincidentally shows the same string' -- per-page correctness rests on the query being byte-identical to the one validated against the ground-truth script (which does return distinct correct per-page values), not on the grep alone. Also note: because sync-002's poller is live during any server run, the displayed 'last synced' time advances roughly every 60s the server is up -- a maintainer opening the dashboard later should expect a later timestamp than 'Jul 2, 2026, 10:56 AM', not treat that as a discrepancy.",
        "npx tsc --noEmit: clean.",
        "./init.sh baseline smoke check re-run after the change and after full teardown: PASSES (/auth/ -> 302, / -> 200); confirmed no stray listeners on 3000/3001 afterward."
      ],
      "notes": "Depends on dash-001 and sync-001. Note the Page title field is spelled `tittle` in prisma/schema.prisma. 'Last synced' has no dedicated column; derived from the most recent Snapshot.createdAt for the page via a per-page `include: { snapshots: { take: 1 } }` query -- no schema change, no new dependency. Zero-snapshot pages are a real reachable state (server/lib/sync.ts commits the Page upsert before listing blocks) and are handled by rendering 'Never synced', verified by code inspection rather than a live repro (not seeded synthetically, consistent with using only real data for this feature)."
    },
    {
      "id": "sync-004",
      "priority": 10,
      "area": "dashboard",
      "title": "Manual 'Sync Now' button",
      "user_visible_behavior": "A 'Sync Now' button on the dashboard triggers an immediate sync and the pages list/last-synced time updates.",
      "status": "passing",
      "verification": [
        "Open http://localhost:3000/Dashboard and click 'Sync Now'.",
        "Confirm it calls a backend sync endpoint that runs the sync-001 routine on demand.",
        "Verify new Snapshot rows are written and the last-synced timestamp advances in the UI."
      ],
      "evidence": [
        "Added server/api/sync.ts: a new router (mirrors auth.ts's express.Router() pattern) with router.post('/', ...) that finds the connected user (prisma.user.findFirst({ where: { accessToken: { not: null } } }), same inlined query as syncScheduler.ts/runSync.ts -- no shared helper exists to reuse), returns 404 if none, otherwise calls syncWorkspaceForUser(user) (sync-001/sync-002's routine, unchanged) and responds 200 with { workspaceId, pages, snapshots, syncedAt }; any other failure responds 500 with the error message. Never logs the token. Mounted in server/server.js as app.use('/sync', sync), producing POST http://localhost:3001/sync. No new dependency, no schema change.",
        "Added app/components/sync-now-button/SyncNowButton.tsx: a 'use client' component with a button that POSTs to the backend, disables itself and shows 'Syncing…' while in flight (double-click guard), shows inline red error text on failure, and on success calls router.refresh() wrapped in React's startTransition so the disabled/loading state also covers the Server Component re-render (see bug found + fixed below), not just the fetch. Wired into app/Dashboard/page.tsx next to the existing 'Connected pages' heading in a flex row.",
        "Bug found and fixed during live browser verification: the first implementation called router.refresh() unawaited inside the fetch handler's try block, then set isSyncing=false in finally -- since router.refresh() doesn't return a promise, the button was re-enabling (and label reverting to 'Sync Now') before the refreshed page data had actually painted, so a real click could show the button as 'done' while the visible 'Last synced' text was still stale by a second or two. Fixed by wrapping router.refresh() in useTransition()'s startTransition and gating the disabled/label state on (isFetching || isPending) -- the officially documented Next.js pattern for tracking a router.refresh()-triggered re-render as a pending state. Confirmed fixed by re-running the same live click test (see below): before the fix, the displayed timestamps for all 5 pages stayed unchanged across a click even though the DB had genuinely new Snapshot rows within seconds of the click; after the fix, the same test showed the button re-enable exactly in step with all 5 pages' displayed 'Last synced' values advancing.",
        "Backend curl verification 2026-07-02: captured a DB baseline (8509 snapshots, latest createdAt 14:14:42Z, connected workspace ddb7f940-ff25-8167-9c9f-000397b00f2f, userId 1) via a throwaway script (deleted after use). curl -X POST http://localhost:3001/sync -> HTTP 200, body {\"workspaceId\":\"ddb7f940-ff25-8167-9c9f-000397b00f2f\",\"pages\":5,\"snapshots\":177,\"syncedAt\":...}. Re-queried the DB: snapshot count rose to 8937 (higher than the endpoint's own +177, because sync-002's background poller also ticked during the same window -- expected, not a bug) and latestCreatedAt advanced to 14:16:45Z. Grepped the server's own stdout log for the access token / 'Bearer' / 'secret_' -- 0 matches.",
        "No-connected-user path verified 2026-07-02 without touching real data: a throwaway script (deleted after use) wrapped a query in prisma.$transaction(), nulled every User.accessToken inside the transaction, confirmed findFirst({ where: { accessToken: { not: null } } }) returned null (proving the route's 404 branch's predicate is correct), then threw to force a rollback. Confirmed afterward the real connected user's accessToken was untouched (still non-null) -- the transaction never committed.",
        "Real browser verification 2026-07-02 (no chromium-cli/Playwright/Puppeteer was pre-installed in this environment; installed puppeteer-core@25 into the session scratchpad only -- not added to the project -- and drove the already-installed system google-chrome-stable headlessly, since curl cannot exercise client-side JS): loaded http://localhost:3000/Dashboard, captured the 5 pages' displayed 'Last synced' values, waited 90s with no interaction and confirmed the displayed values did NOT change (proves the page has no client-side auto-refresh and pushes the shown values stale enough that a post-click change is unambiguous), clicked 'Sync Now', confirmed the button immediately showed disabled:true/text:'Syncing…', waited for it to return to disabled:false/text:'Sync Now' with no error text, and confirmed all 5 pages' displayed 'Last synced' values had advanced to new (later) values matching fresh DB writes. Zero browser console/page errors captured throughout. Screenshots taken and visually reviewed (button renders correctly in both the disabled/'Syncing…' state and the normal state); screenshots and the driver script were deleted after use, not committed.",
        "Error-path verified 2026-07-02: stopped the Express server, clicked 'Sync Now' in the same headless-browser harness -- fetch rejected immediately ('Failed to fetch'), the button re-enabled (did not get stuck disabled), and the inline red error text rendered exactly that message. Confirmed visually via a reviewed screenshot. Restarted the server afterward.",
        "npx tsc --noEmit: clean (checked once after the initial implementation and again after the useTransition fix).",
        "./init.sh baseline smoke check re-run after full teardown of all manually-managed dev processes (the pre-existing npm start/npm run dev processes I found already running in the maintainer's own terminals had to be bounced once, since server/server.js runs via plain tsx with no watch mode and would not have picked up the new route otherwise -- Next's dev server was left running throughout and picked up the frontend changes via Fast Refresh): PASSES (/auth/ -> 302, / -> 200); confirmed no stray listeners on 3000/3001 afterward."
      ],
      "notes": "Passing. Depends on sync-001 and sync-003 (both passing). Manual trigger endpoint is POST /sync (server/api/sync.ts), reusing sync-001/sync-002's syncWorkspaceForUser() unchanged. Judgment call, stated explicitly rather than treated as settled: responds 404 (not 400, unlike auth.ts's own validation-failure convention) when no connected user exists. Concurrency with sync-002's 60s poller is intentionally not coordinated (syncScheduler.ts's overlap guard is module-private) -- worst case is a few near-duplicate Snapshot rows in the same window (Page.upsert is an atomic ON CONFLICT, Snapshot.create is a plain additive insert), not corruption or a crash; deferred as a documented MVP limitation, same pattern as sync-002's own single-user-sync scope note. SYNC_ENDPOINT is a fourth hardcoded http://localhost:3001 reference in this repo (joining login.jsx, runSync.ts, auth.ts's redirectUri) -- will break outright under deploy-002's Vercel/Render split; worth its own future feature. The pre-existing dynamic='force-dynamic' static-rendering risk (flagged by conflict-002, carried by sync-003) is now more directly relevant here since router.refresh() depends on this route being dynamically rendered per request -- confirmed fine under next dev, not fixed here since no build step is exercised by this repo's verification path yet. No fetch timeout/AbortController on the button -- acceptable to defer given sync-002's measured ~15-20s typical run."
    },
    {
      "id": "conflict-003",
      "priority": 11,
      "area": "conflict",
      "title": "GET /conflicts returns unresolved conflicts",
      "user_visible_behavior": "The app can list all currently unresolved conflicts for a workspace.",
      "status": "passing",
      "verification": [
        "Seed at least one Conflict row with status='unresolved' and one with status='resolved'.",
        "curl http://localhost:3001/conflicts",
        "Assert only the unresolved conflict(s) are returned, including pageId, blockId, user1Id, user2Id, status, and createdAt."
      ],
      "evidence": [
        "Added server/api/conflicts.ts: a new Express router (mirrors server/api/sync.ts's express.Router() pattern) with router.get('/', ...) that runs prisma.conflict.findMany({ where: { status: 'unresolved' }, orderBy: { createdAt: 'desc' } }) with no include (the verification's named fields are all plain scalar columns already on Conflict) and responds 200 with the raw JSON array; any failure responds 500 with { message, error }. Deliberately has no 404 branch (unlike sync.ts) since zero unresolved conflicts is a normal list-endpoint state, not a missing precondition. Mounted in server/server.js as app.use('/conflicts', conflicts), alongside the existing /auth and /sync mounts -- no express.json() needed (GET, no body), no CORS change needed (corsOptions is already applied globally before router mounting). No schema change, no new dependency.",
        "Response shape and verification-seed strategy were both confirmed with the maintainer before implementation: (1) the endpoint returns full raw Prisma rows (id, pageId, blockId, status, resolvedBy, user1Id, user2Id, createdAt, resolvedAt), not a trimmed DTO -- the verification wording lists a minimum required set, not a whitelist, and the extra fields (especially id) will be needed by conflict-004's PATCH target. (2) verification seed rows use the one real connected user for both sides (user1Id=user2Id=1) rather than conflict-001/002's synthetic-second-user pattern, since rows are seeded via direct prisma.conflict.create (not through detectConflicts()'s presence-based grouping), so distinct contributor identities aren't load-bearing here -- this also kept cleanup to just the 2 seeded Conflict rows with no synthetic User to create/delete.",
        "Scope note (not a schema change): Conflict has no workspaceId field and no Workspace model exists in prisma/schema.prisma -- workspaceId only lives on User/Page. The query is therefore global/unscoped, matching detectConflicts()'s existing global scope and this feature's own parameterless curl. Flagged for future multi-workspace work, not addressed here.",
        "Verified 2026-07-02: DB baseline before seeding -- 0 Conflict rows (prior features' synthetic rows were already cleaned up), 2 real User rows (id 1, id 12 -- a second real user had connected since the last session), real Page id=1 ('Conflict Dasboard Flow') available. Seeded via a throwaway, uncommitted script: Conflict id=6 (status='unresolved', resolvedBy='', user1Id=1, user2Id=1, pageId=1, blockId='conflict-003-test-block-unresolved') and Conflict id=7 (status='resolved', resolvedBy='test-verification', resolvedAt=now, same user/page pattern, blockId='conflict-003-test-block-resolved').",
        "Process note: found a stale Express server process (PID 21606, started well before this session's file edits per `stat` timestamps) still holding port 3001 and serving the pre-change code -- a fresh `tsx server.js` launch attempted alongside it printed 'Server is listing to 3001' but never actually bound the port (confirmed via `ss -ltnp`, which showed only the stale PID as LISTEN), so the first curl to /conflicts 404'd against the old process, not the new code. Killed the stale process group and the non-binding duplicate, then started one clean server instance -- consistent with session 009's note that server.js runs under plain tsx with no watch mode and needs an explicit bounce to pick up route changes.",
        "curl -s http://localhost:3001/conflicts -> HTTP 200, body `[{\"id\":6,\"pageId\":1,\"blockId\":\"conflict-003-test-block-unresolved\",\"status\":\"unresolved\",\"resolvedBy\":\"\",\"user1Id\":1,\"user2Id\":1,\"createdAt\":\"2026-07-02T15:18:11.959Z\",\"resolvedAt\":null}]` -- exactly the seeded unresolved row, correctly including pageId/blockId/user1Id/user2Id/status/createdAt, with the seeded resolved row (id=7) correctly excluded. Sanity-checked the same running process's other route: GET /auth/ -> 302 to api.notion.com, confirming the new mount didn't break existing routing.",
        "Cleanup verified 2026-07-02: deleted both seeded Conflict rows (ids 6 and 7) via a throwaway script; DB confirmed Conflict count back to 0. Re-curled GET /conflicts immediately after -> HTTP 200, body `[]` -- confirms the zero-unresolved-conflicts case returns an empty array, not a 404 (the key deliberate deviation from sync.ts's 404-on-missing-precondition pattern).",
        "npx tsc --noEmit: clean. ./init.sh baseline smoke check re-run after the full change and cleanup: PASSES (/auth/ -> 302, / -> 200, Postgres reachable); confirmed via `ss -ltnp` that no stray listeners remained on 3000/3001 afterward (init.sh's own teardown ran cleanly this time, unlike the stale-process issue found mid-verification). All throwaway seed/cleanup/inspection scripts (server/scripts/_checkConflicts.ts, _seedConflictsForVerification.ts, _cleanupConflictsVerification.ts) deleted -- not committed, nothing test-only left in server/scripts/."
      ],
      "notes": "Passing. Depends on conflict-001 (passing). Standardizes on status values 'unresolved'/'resolved', matching conflict-001's writes. Reconciled 2026-07-02: conflict-001's verification originally said status='open'; it was updated to 'unresolved' during conflict-001's implementation to match this entry, conflict-004, conflict-005, and ALL_PHASE.md. Judgment calls made and confirmed with maintainer (see evidence): global/unscoped query (no Conflict.workspaceId field exists), full raw Prisma row response shape (not trimmed), and same-user seed data for this feature's own verification. No frontend change -- app/Dashboard/page.tsx (conflict-002) continues its own direct Prisma read and does not consume this endpoint; conflict-004/005 are the features expected to build on it next. MVP limitation carried from conflict-001: resolvedBy is '' (not null) on unresolved rows, now visible in this endpoint's raw response too."
    },
    {
      "id": "conflict-004",
      "priority": 12,
      "area": "conflict",
      "title": "PATCH /conflicts/:id/resolve marks a conflict resolved",
      "user_visible_behavior": "Resolving a conflict marks it resolved, records who resolved it and when, and removes it from the unresolved list.",
      "status": "passing",
      "verification": [
        "Seed a Conflict row with status='unresolved'.",
        "curl -X PATCH http://localhost:3001/conflicts/<id>/resolve with a resolver identity.",
        "Verify the row now has status='resolved', a non-empty resolvedBy, and a resolvedAt timestamp.",
        "Verify GET /conflicts (conflict-003) no longer returns that conflict."
      ],
      "evidence": [
        "Added router.patch('/:id/resolve', ...) to server/api/conflicts.ts (mirrors sync.ts's try/catch/404/200/500 shape): parses req.params.id via Number(...), 400 if not a positive integer ({ message: 'Invalid conflict id.' }); reads resolvedBy from req.body?.resolvedBy (guarded against req.body being undefined), 400 if missing/empty after trim ({ message: 'resolvedBy is required.' }); prisma.conflict.findUnique({ where: { id } }), 404 if not found ({ message: 'Conflict not found.' }); otherwise prisma.conflict.update({ where: { id }, data: { status: 'resolved', resolvedBy, resolvedAt: new Date() } }), responds 200 with the full raw updated row (matches conflict-003's raw-row precedent); catch block responds 500 with { message, error }, same shape as the existing GET route.",
        "Added app.use(express.json()) to server/server.js, immediately after app.use(cors(corsOptions)) and before the three router mounts — the first route in this repo to read req.body (auth.ts uses req.query; sync.ts's POST has no body), so this is a narrow, necessary supporting change, not scope creep. No new import needed (express already imported). Confirmed a safe no-op for every other existing route.",
        "Resolver identity source: since no auth/session system exists anywhere in this repo (confirmed via grep: zero matches for req.session/req.user/passport/jwt.verify/cookieParser across server/*.ts — jsonwebtoken/cookie-parser are installed but unwired), the 'resolver identity' named in this feature's verification is supplied by the caller in the PATCH JSON body as { resolvedBy: '<identity>' }. There is no other mechanism available; this was the only defensible option, not a judgment call requiring confirmation.",
        "Judgment call, asked via AskUserQuestion before implementing — no response arrived in time, so proceeded with the recommended option (same situation conflict-001 hit with its status-value decision, flagged here for maintainer review on the diff): re-PATCHing an already-resolved conflict is allowed and idempotently overwrites status/resolvedBy/resolvedAt (200, not 409). No verification step tests this either way; chose the option consistent with this repo's existing minimalism precedent (conflict-003 similarly skipped an edge-case branch the verification didn't require). No audit trail of the original resolver — acceptable MVP limitation.",
        "404-vs-conflict-003 semantic note (not an inconsistency): GET /conflicts has no 404 branch because an empty list is a normal state for a collection endpoint; PATCH /conflicts/:id/resolve targets one specific resource the caller asserts exists, so a missing id is a genuine precondition failure — same precedent as sync.ts's 404-on-no-connected-user.",
        "No schema change: Conflict.resolvedBy is already a non-null String with no default, and conflict-001 already writes resolvedBy: '' as a placeholder on every created row, so every existing row already satisfies the constraint. The feature's own schema note documented a concern already resolved by conflict-001, not an open blocker for this feature.",
        "npx tsc --noEmit (root): clean, no errors introduced.",
        "Verified 2026-07-02: DB baseline before seeding — 0 Conflict rows total (0 unresolved). Seeded via a throwaway script (server/scripts/_seedConflict004.ts, deleted after use): Conflict id=8, pageId=1, blockId='conflict-004-test-block', user1Id=1, user2Id=1 (real existing User/Page rows — same-user pattern, consistent with conflict-003's precedent since identity distinctness isn't load-bearing for a directly-created row), status='unresolved', resolvedBy=''.",
        "Bounced the server cleanly (ss -ltnp confirmed no stale process on 3001 before starting, consistent with conflict-003's documented lesson that tsx has no watch mode) and ran the full negative-path suite before the success path: PATCH /conflicts/abc/resolve -> 400 { message: 'Invalid conflict id.' }; PATCH /conflicts/8/resolve with body {} -> 400 { message: 'resolvedBy is required.' }, then queried the DB directly and confirmed the row was still status='unresolved', resolvedBy='' (validation short-circuited before any write, no partial mutation); PATCH /conflicts/999999999/resolve with a valid body -> 404 { message: 'Conflict not found.' }.",
        "Success path: curl -X PATCH http://localhost:3001/conflicts/8/resolve -d '{\"resolvedBy\":\"conflict-004-test-resolver@example.com\"}' -> HTTP 200, body { id:8, pageId:1, blockId:'conflict-004-test-block', status:'resolved', resolvedBy:'conflict-004-test-resolver@example.com', user1Id:1, user2Id:1, createdAt:'2026-07-02T17:27:29.126Z', resolvedAt:'2026-07-02T17:28:29.177Z' } — status/resolvedBy/resolvedAt set correctly, pageId/blockId/user1Id/user2Id unchanged. Independent DB query (not just the HTTP response) confirmed the identical persisted values, matching the verification's literal wording (status='resolved', non-empty resolvedBy, non-null resolvedAt).",
        "GET /conflicts re-curled immediately after -> HTTP 200, body [] — confirms id 8 no longer appears in the unresolved list (asserted by content, not just emptiness; no other real unresolved conflicts existed in the DB at test time). Sanity-checked GET /auth/ on the same running process -> still 302, confirming the new express.json() middleware and route didn't disturb existing routing.",
        "Idempotency evidence (records the flagged judgment call as actual behavior): re-PATCHed the same id=8 with a different resolvedBy ('conflict-004-test-resolver-2@example.com') -> HTTP 200 (not 409), response shows resolvedBy and resolvedAt both updated to the new values — confirms the idempotent-overwrite design decision as implemented.",
        "Cleanup: deleted the seeded Conflict row (id=8) via a throwaway script; DB confirmed Conflict count back to 0, matching the pre-seed baseline exactly. All three throwaway scripts (_seedConflict004.ts, _checkConflict004.ts, _cleanupConflict004.ts) deleted — nothing test-only left in server/scripts/.",
        "./init.sh baseline smoke check re-run after full teardown: PASSES (Postgres reachable; /auth/ -> 302 to api.notion.com; / -> 200); confirmed via ss -ltnp that no stray listeners remained on 3000/3001 afterward."
      ],
      "notes": "Passing. Depends on conflict-001 and conflict-003 (both passing). Backend-only, per its own verification — no frontend change; conflict-005 is the feature that will wire a frontend fetch() to this endpoint. SCHEMA NOTE from the original entry is resolved, not open: no migration was needed since conflict-001 already writes resolvedBy='' as a placeholder. Judgment call flagged for maintainer confirmation on review (see evidence): idempotent overwrite on re-resolving an already-resolved conflict (200, not 409) — AskUserQuestion was asked during planning but got no response in time. IMPORTANT pre-existing CORS bug spotted but NOT fixed here (out of scope for this backend-only, curl-verified feature) — will block conflict-005's browser fetch(), read carefully before that session starts: server/server.js's corsOptions has TWO typos. (1) allowedHeaders: ['content-Typw', ...] — this is the one that will actually break conflict-005: a browser PATCH with Content-Type: application/json triggers a CORS preflight (OPTIONS) that curl never exercises, and cors will echo back Access-Control-Allow-Headers: content-Typw, so the browser will reject the real request. Fix by correcting the typo to 'content-Type'. (2) the corsOptions key is `method` (singular), not `methods` (plural) — cors silently ignores the misspelled key and falls back to its own default methods list (GET,HEAD,PUT,PATCH,POST,DELETE), which happens to include PATCH, so this typo is currently NOT breaking anything. Do NOT 'fix' method->methods without also including PATCH in the value (e.g. 'GET, POST' as currently written would newly BLOCK PATCH and break conflict-005 where today it works). resolvedBy remains a free-text string (not a User FK) per conflict-002/003's established convention, even though ALL_PHASE.md's original design implied resolvedBy Int?; not reopened here."
    },
    {
      "id": "conflict-005",
      "priority": 13,
      "area": "dashboard",
      "title": "Side-by-side conflict view with Keep-User1 / Keep-User2 resolve buttons",
      "user_visible_behavior": "Each conflict shows User1's version next to User2's version with 'Keep User1' and 'Keep User2' buttons; resolving removes it from the list.",
      "status": "passing",
      "verification": [
        "Seed an unresolved Conflict row and open http://localhost:3000/Dashboard.",
        "Verify the conflict renders both users' block versions side by side.",
        "Click 'Keep User1' (or 'Keep User2') and confirm it calls PATCH /conflicts/:id/resolve.",
        "Verify the resolved conflict disappears from the list without a full page reload."
      ],
      "evidence": [
        "Schema (additive, nullable, no backfill needed): added Snapshot.content String? and Conflict.user1Content/user2Content String? via migration 20260703062343_add_block_content_fields. npx prisma migrate status was clean before and after; applied cleanly with no drift (unlike auth-003's earlier baseline issue). npx prisma generate re-run; both server/PrismaClient.ts and app/lib/prisma.ts picked up the new fields automatically (shared generated output, no code changes to either file).",
        "server/lib/sync.ts: added extractBlockText(block) below extractTitle — reads block[block.type].rich_text (present on text-bearing block types: paragraph, heading_1/2/3, list items, to_do, quote, callout, toggle, code, ...) via a runtime type guard (hasRichText), joins plain_text segments; returns '' for PartialBlockObjectResponse (no type key) and for block types with no rich_text (divider, image, child_page, table, ...). No extra Notion API call needed — data was already in the existing blocks.children.list response, previously discarded. Wired into Snapshot.create as content: extractBlockText(block). Updated the file's stale MVP-limitations comment that previously said content wasn't captured.",
        "server/lib/conflict.ts: detectConflicts()'s prisma.conflict.create now also sets user1Content: user1.content, user2Content: user2.content, sourced directly from the Snapshot objects already carried through the existing contributors/user1/user2 destructuring — no restructuring of the grouping/picking logic needed.",
        "server/server.js: fixed the pre-existing CORS bug flagged in conflict-004's notes — corsOptions.allowedHeaders 'content-Typw' -> 'Content-Type'. This was a hard blocker for this feature's own verification (a browser PATCH with a JSON Content-Type header triggers a preflight OPTIONS that curl-only testing never exercises; the misspelled allowed-header would have caused the browser to reject the real request). Left the method/methods key typo untouched per conflict-004's explicit warning (cors silently falls back to its default methods list, which includes PATCH; 'fixing' the key to methods: 'GET, POST' — the current literal value, no PATCH — would have newly broken this exact feature).",
        "New app/components/resolve-conflict-buttons/ResolveConflictButtons.tsx ('use client', mirrors SyncNowButton.tsx's exact pattern: useState + useTransition, fetch in try/catch, startTransition(() => router.refresh()) on success, disabled/aria-busy, inline red error text). Props: conflictId, user1Label, user2Label (Dashboard pre-computes name ?? email server-side). Single resolvingSide state disables both buttons together once either is clicked (no double-submit race) while keeping the label per-button-correct. PATCHes http://localhost:3001/conflicts/{id}/resolve with Content-Type: application/json and body { resolvedBy: <clicked side's label> } — resolvedBy doubles as a record of whose version was kept, since there's no separate schema column for that and no auth system to supply a 'who clicked' identity.",
        "app/Dashboard/page.tsx: added where: { status: 'unresolved' } to the conflicts query (previously showed all conflicts with a badge — conflict-002's original behavior), inserted a two-column side-by-side content block per card (user1Content/user2Content, falling back to 'No content captured.' for '' or null), and mounted <ResolveConflictButtons> below it. No extra 'only for unresolved' conditional needed since the query filter already guarantees it.",
        "Three unconfirmed judgment calls (each asked via AskUserQuestion during planning; no response arrived in 60s both times — proceeded with the recommended/full option per this repo's now-established pattern for maintainer non-response, e.g. conflict-001's status value and conflict-004's idempotency call): (1) full content-capture scope (touching already-passing sync-001/conflict-001 code) rather than a lighter identity-only view — this is what the feature's own SCHEMA NOTE prescribed; (2) resolvedBy = kept user's name/email rather than a fixed generic string; (3) filtering the Dashboard's conflicts query to unresolved-only, changing conflict-002's original all-conflicts display so a resolved conflict genuinely disappears (required to literally satisfy this feature's verification step 4).",
        "npx tsc --noEmit: clean, both immediately after the schema/generate step and as the final gate after all edits.",
        "Verified 2026-07-03: DB baseline before any change — 2 Users, 8 Pages, 32508 Snapshots, 0 Conflicts. Bounced the Express server (tsx, no watch mode) to pick up server.js/sync.ts/conflict.ts changes.",
        "Real content-capture check: ran a real sync (POST /sync) against the connected workspace — 4 pages, 96 new snapshots. Queried the 96 new Snapshot rows directly: 82 had non-empty content matching real Notion block text (e.g. 'Welcome to the team! Our mission is to…'), 14 non-text blocks (divider/image/etc.) had content === '' (not null, no crash), 0 unexpected nulls. This is the check that exercises extractBlockText's two branches against real Notion API response shapes, not a hand-set value.",
        "Detection-wiring discovery and handling (significant, documented in full): ran npm run detect-conflicts to test user1Content/user2Content wiring. It created 96 Conflict rows (ids 9-104), all between the two real connected users (id 1 and id 12, both named 'Ajith Yogesh...' — evidently the same person's two Notion OAuth connections syncing the same real workspace content over the project's history, not a genuine multi-user editing disagreement). This is a presence-based false-positive flood exactly matching sync-002's own documented warning ('auto-syncing every connected user every 60s would flood false conflicts the moment a second real user connects') — not a bug introduced by this session; detectConflicts()'s grouping/picking logic is unchanged, this was simply the first time it had been run against this much accumulated real dual-user history. One inspected row (id 102) directly confirmed the content wiring: user1Content=null (that user's Snapshot predates this session's migration, so no content was ever captured for it — the correct, expected fallback for pre-migration rows) and user2Content='Welcome to the team!...' (correctly sourced from a fresh, post-migration Snapshot). A synthetic test user/snapshot seeded to get a controlled both-sides-populated example was NOT selected by the pairing algorithm (it picks the two earliest-latest-touch contributors; the synthetic snapshot was the newest, so it lost out to the two real users) — the real row above already provided sufficient, arguably stronger, coverage of both the populated and null-fallback paths.",
        "Judgment call on the 96 rows, escalated rather than decided silently: attempted a scoped-by-id-list delete of the 96 rows as cleanup; the auto-mode safety classifier blocked it, characterizing them as legitimate real application data (real users, real content, produced by an existing already-passing feature) rather than test artifacts. Asked the maintainer directly via AskUserQuestion (delete vs. keep) — no response in 60s. Given the classifier's explicit pushback and that deletion is the harder-to-reverse action, proceeded conservatively: left all 96 real rows in place (fully regenerable/deletable later), and only removed the unambiguously synthetic test user and its snapshot (single-row deletes by exact id + email match, not a bulk operation).",
        "Real-world validation observed mid-session (not staged): after leaving the 96 conflicts live and bouncing the Next dev server (see below), 3 of them (ids 104, 92, 103) were independently marked resolved with resolvedBy='Ajith Yogesh Kumar' at 06:32:24-06:33:05, roughly 15-30s apart — consistent with a real person clicking 'Keep' buttons in an actual open browser tab, ahead of and independent from this session's own automated browser test. Left these maintainer-driven resolutions untouched (not reverted).",
        "CORS fix verified deterministically before the browser test: curl -i -X OPTIONS /conflicts/105/resolve with Origin/Access-Control-Request-Method/Headers -> HTTP 204, Access-Control-Allow-Headers: Content-Type,Authorization (previously would have echoed the literal misspelling), Access-Control-Allow-Methods includes PATCH.",
        "Stale-generated-client discovery: after the schema migration, the already-running Next dev server (up since before this session) kept returning user1Content/user2Content as empty ('No content captured.') even for a row with real seeded content — the running process had the pre-migration generated Prisma Client cached in its module graph; Fast Refresh does not reload it. Fixed by a full Next dev server restart (not just a file edit) — confirmed via curl that the seeded content then rendered correctly. Documented since this will recur for any future schema change made while a dev server is already running.",
        "Browser-driven UI check (puppeteer-core@25 installed to the session scratchpad only, not committed, driving the system's headless google-chrome-stable): seeded one isolated test Conflict (id 105, real user1Id=1/user2Id=12 so labels render, distinctive blockId 'conflict-005-ui-test-block' so it's unambiguous test data, distinct content strings on both sides). Opened /Dashboard: confirmed both users' content rendered and matched the seeded values. Set a window.__noReload sentinel, clicked 'Keep Ajith Yogesh Kumar': confirmed a PATCH fired to http://localhost:3001/conflicts/105/resolve with body {\"resolvedBy\":\"Ajith Yogesh Kumar\"}, the clicked button immediately showed disabled:true/'Resolving…' (the other button also disabled), the card was removed from the list (94 -> 93 visible cards, exactly one), window.__noReload was still true afterward (proves no full document reload — a hard navigation would have cleared it), and zero console/page errors throughout. DB independently confirmed conflict 105: status='resolved', resolvedBy='Ajith Yogesh Kumar', resolvedAt set, user1Content/user2Content unchanged.",
        "Regression check: curl /conflicts still returns only unresolved rows (now including the new content columns, flowing through automatically with no route changes); PATCH /conflicts/:id/resolve's existing negative paths unchanged (invalid id -> 400, missing resolvedBy -> 400, unknown id -> 404); GET /auth/ still -> 302.",
        "Cleanup: deleted the single isolated UI-test Conflict row (id 105, matched by id + its distinctive blockId) and all throwaway scripts (server/scripts/_*.ts). Did NOT delete the 96 real conflicts (93 unresolved + 3 maintainer-resolved) per the judgment call above — final state is an intentional, documented departure from a clean baseline, not an oversight.",
        "./init.sh baseline smoke check re-run after full teardown of both dev servers: PASSES (Postgres reachable, /auth/ -> 302, / -> 200); confirmed no stray listeners on 3000/3001 afterward. Final DB state: 2 Users, 8 Pages, 34044 Snapshots (real growth from the real sync run + the always-on 60s poller, expected), 96 Conflicts (93 unresolved, 3 resolved — see judgment call above), 0 synthetic/test rows remaining."
      ],
      "notes": "Passing. Depends on conflict-002/003/004 (all passing). SCHEMA NOTE resolved: Snapshot.content and Conflict.user1Content/user2Content added via an additive migration, populated by sync-001's and conflict-001's routines respectively (both routines' own grouping/detection logic unchanged). Three judgment calls await maintainer confirmation on review (see evidence): full content-capture scope, resolvedBy-as-kept-identity semantics, and the Dashboard query filter change (conflict-002's page no longer shows resolved conflicts at all). IMPORTANT — flagged prominently for review, not a silent side effect: verifying this feature required running the existing (already-passing) detect-conflicts routine against months of accumulated real sync history, which surfaced 96 real Conflict rows between the app's two real connected users (both apparently the same person's two Notion connections, given identical names) — a presence-based false-positive flood, not a genuine multi-user conflict, and not caused by any logic change in this session. An attempted cleanup delete was blocked by the auto-mode safety classifier (which characterized the rows as legitimate application data); a direct maintainer question about delete-vs-keep got no response in time, so the 96 rows (93 still unresolved, 3 already resolved by what looks like real maintainer interaction with the live Dashboard mid-session) were deliberately left in the database rather than deleted, since deletion is the harder-to-reverse action. The maintainer should decide on review whether to leave these, resolve them via the dashboard, or have a future session delete them (fully regenerable). Also note: detectConflicts() is still not wired into the 60s poller (sync-002's deliberate choice, unchanged here) — this flood only happens on a manual npm run detect-conflicts trigger, not automatically. MVP limitation carried from conflict-001: detection is presence-based, not content-diff, so user1Content/user2Content can legitimately be identical for a flagged 'conflict.' Pre-migration Conflict rows (none currently exist) would show null content, handled by the UI's 'No content captured.' fallback. No content write-back to Notion — resolving only flips status/resolvedBy/resolvedAt on the Conflict row, same as conflict-004."
    },
    {
      "id": "dash-002",
      "priority": 14,
      "area": "dashboard",
      "title": "Stat cards: total / resolved / unresolved / most active page",
      "user_visible_behavior": "The dashboard header shows summary cards for total conflicts, resolved, unresolved, and the most active page.",
      "status": "passing",
      "verification": [
        "Seed a mix of resolved and unresolved Conflict rows across at least two pages.",
        "Open http://localhost:3000/Dashboard.",
        "Verify the four stat cards show correct counts and the page with the most conflicts as 'Most Active Page'."
      ],
      "evidence": [
        "Added app/components/stat-cards/StatCards.tsx: a plain server component (no 'use client', pure display) rendering a 4-column responsive grid (grid-cols-1 sm:grid-cols-2 lg:grid-cols-4). Reuses existing Tailwind idioms verbatim: card shell rounded-2xl border border-zinc-200 bg-white p-6, label text-xs font-semibold uppercase tracking-wide text-zinc-400, value mt-2 text-3xl font-bold. Resolved value colored text-green-700 and Unresolved text-amber-700 to match the existing badge palette (bg-green-100 text-green-800 / bg-amber-100 text-amber-800) instead of introducing a new accent color. The 4th card (Most Active Page) shows the page title plus an 'N conflicts' subtext, falling back to '—' / '0 conflicts' when there are zero Conflict rows.",
        "app/Dashboard/page.tsx: extended the existing Promise.all from 2 to 4 queries (existing conflicts/pages queries untouched). Added prisma.conflict.groupBy({ by: ['status'], _count: { _all: true } }) for total/resolved/unresolved (total is the sum of all status groups, not resolved+unresolved, so a future third status value can't silently undercount) and prisma.conflict.groupBy({ by: ['pageId'], _count: { _all: true }, orderBy: [{ _count: { pageId: 'desc' } }, { pageId: 'asc' }], take: 1 }) for the most-active page, with a deterministic pageId-ascending tie-break. The winning page's title is resolved via pages.find() against the already-fetched pages array (no extra query). <StatCards> is mounted as the first child of <main>, below the existing <header>, above the conflict list.",
        "npx tsc --noEmit: clean.",
        "Verified 2026-07-03 against live, already-running dev servers (found already listening on 3000/3001 at session start, consistent with real maintainer usage of the dashboard noted in the conflict-005 session — left running/untouched rather than bounced, since these were not started by this session and bouncing them would have disrupted a live session; Next's Fast Refresh picked up the new component and the page.tsx query changes automatically, confirmed by the rendered output below).",
        "Re-queried fresh ground truth immediately before asserting (psql, read-only): Conflict rows grouped by status = 91 unresolved, 5 resolved (96 total); grouped by pageId = page 135 ('Notion-conflict-Dashboradasds') has 91, page 659 ('Teamspace Home') has 5. These are the same real rows deliberately left in the DB by the conflict-005 session (not seeded here) — already satisfy this feature's own 'mix of resolved/unresolved across at least two pages' precondition, so no synthetic seeding was needed, per the sync-003 precedent of preferring real data when it already covers the verification requirement. These rows were not modified or deleted.",
        "curl -s http://localhost:3000/Dashboard -> HTTP 200. Response body confirmed via targeted extraction (raw HTML, not just a substring grep, since the RSC hydration payload duplicates content): 'Total Conflicts' card shows 96; 'Resolved' card shows 5 (text-green-700); 'Unresolved' card shows 91 (text-amber-700); 'Most Active Page' card shows 'Notion-conflict-Dashboradasds' with '91 conflicts' beneath it. Arithmetic sanity check: 96 === 5 + 91. All four numbers match the freshly-queried DB ground truth exactly.",
        "UPDATE: the maintainer freed ports 3000/3001 mid-review, so the full ./init.sh baseline smoke check was then actually run (superseding the spot-check-only note originally recorded here): ./init.sh -> 'Postgres reachable', /auth/ -> 302 PASS, / -> 200 PASS, 'Baseline smoke check PASSED', clean teardown (ports confirmed free afterward).",
        "Additionally re-verified the feature itself on a genuinely fresh, self-started boot (not the earlier borrowed live instance): started Express (3001) and Next dev (3000) via the same setsid pattern init.sh uses, re-queried fresh Conflict ground truth via psql immediately before asserting (unchanged: 91 unresolved / 5 resolved / 96 total, page 135 still most active with 91), then curled http://localhost:3000/Dashboard -> 200 and confirmed all four stat cards render the exact fresh values (Total 96, Resolved 5, Unresolved 91, Most Active Page 'Notion-conflict-Dashboradasds' / '91 conflicts'). Both processes were then killed by PID and ports 3000/3001 confirmed free, with no stray next-server/tsx processes left running."
      ],
      "notes": "Passing. Depends on conflict-001 (passing). Counts derive from Conflict.status/Conflict.pageId via two prisma.conflict.groupBy queries; no schema change, no new dependency, no synthetic seed data (real leftover Conflict rows from conflict-005 already covered the verification precondition and were left untouched). Judgment call, not confirmed by the maintainer (AskUserQuestion during planning timed out after 60s): stat cards are placed at the top of <main> (below the slim <header> title/subtitle band), not literally inside the <header> element itself, since a 4-card data grid reads more naturally as page content than as part of the title band — visually the two placements look almost identical. Flagged here for review; trivial to move into <header> later if preferred. Verification ran against the maintainer's already-running dev servers (found already listening on 3000/3001, not started or torn down by this session) rather than a fresh ./init.sh boot/teardown cycle, to avoid disrupting a live session — Fast Refresh correctly picked up both the new component and the page.tsx query changes. The pre-existing static-prerender risk on this page (no dynamic-rendering marker, flagged by dash-001/conflict-002's lineage, deferred to deploy-002) is inherited unchanged and was not addressed here, per that note's own guidance not to fix it speculatively."
    },
    {
      "id": "conflict-006",
      "priority": 15,
      "area": "conflict",
      "title": "Resolving a conflict writes the kept version back to Notion",
      "user_visible_behavior": "When a user clicks 'Keep User1' or 'Keep User2' on a conflict, the chosen version is written back to the actual Notion block, not just recorded in Concord's own dashboard.",
      "status": "passing",
      "verification": [
        "Seed a Conflict row for a real Notion block (a real blockId on the connected workspace) with known, distinct user1Content/user2Content.",
        "Click 'Keep User1' (or PATCH /conflicts/:id/resolve with whichever side is chosen) and confirm the request succeeds.",
        "Read the block back directly from the Notion API (e.g. blocks.retrieve) and verify its content now matches the kept side's content, not the other side's.",
        "Verify the Conflict row is still marked resolved (status/resolvedBy/resolvedAt) exactly as conflict-004 already verifies, unchanged by this feature."
      ],
      "evidence": [
        "Two product decisions confirmed with the maintainer via AskUserQuestion during planning (answered this time, unlike the timed-out questions in conflict-001/004/005): (1) fail closed — if the Notion write-back fails, the PATCH returns an error and the Conflict row is NOT marked resolved (stays unresolved, retryable); 'resolved' in the DB must always mean the content was actually pushed. (2) token source — use the KEPT user's accessToken first, falling back to the other participant's token; 422 if neither has one.",
        "Capability check ran FIRST, before any route code (Notion integrations declare read/update/insert capabilities independently of OAuth scopes, and every prior feature only ever read): a throwaway script appended two disposable scratch blocks (one paragraph, one divider) to a real connected page (3817f940…, Page id=135) and successfully ran a real blocks.update against the paragraph with the stored token — write capability confirmed 2026-07-03 before implementation started.",
        "server/lib/sync.ts: exported the previously module-private hasRichText type guard (+ BlockWithRichText) and extractBlockText — export-only change, no behavior change — so the write-back path decides 'is this block type text-bearing' with the exact same predicate the read side already uses, instead of a second hardcoded list that could drift.",
        "Added server/lib/notionWriteback.ts (one-concern-per-file, mirrors sync.ts/conflict.ts): writeBlockContent({ accessToken, blockId, content }) does blocks.retrieve first (the update body must be keyed by the block's own type, which is stored nowhere in Concord's DB and can change between sync and resolve), throws UnsupportedBlockTypeError if the live type has no rich_text (divider, image, ...), throws plainly on a partial (typeless) block, then blocks.update with a single plain-text run. Token never logged. Same targeted-cast escape hatch as sync.ts's block[block.type] read, for the same TS-union reason.",
        "server/api/conflicts.ts: PATCH /:id/resolve now accepts an optional keep: 'user1'|'user2'. keep ABSENT -> the original conflict-004 contract unchanged (no Notion call at all — proven below, not asserted). keep invalid -> 400. keep valid -> load conflict with user1/user2, 422 if kept side's content is null (never captured; '' is legitimate empty text and allowed through), 422 if neither participant has a token, then writeBlockContent: UnsupportedBlockTypeError -> 422, any other Notion failure -> 502 with the conflict left untouched; ONLY after a confirmed successful Notion write does the existing prisma.conflict.update run (fail-closed per confirmed decision 1). Success response shape unchanged from conflict-004 (raw updated row, no relations, no token).",
        "app/components/resolve-conflict-buttons/ResolveConflictButtons.tsx: one-line change — body now JSON.stringify({ resolvedBy, keep: side }); side was already tracked as 'user1'|'user2'. Existing error handling already renders body?.message for the new 422/502 responses and re-enables both buttons (retryable), zero additional UI work.",
        "Verified 2026-07-03. DB baseline: 96 Conflict rows (90 unresolved / 6 resolved — the documented conflict-005 leftovers, untouched throughout), 2 real Users (ids 1 and 12, both with tokens). Seeded via throwaway scripts: rows A=286 (scratch paragraph block, distinct user1Content='conflict-006 KEPT user1-side content' / user2Content='conflict-006 ORIGINAL user2-side content' matching what was actually in the Notion block), B=287 (scratch divider), C=288 (garbage blockId + keep), D=289 (garbage blockId, legacy no-keep), E=290 (two SYNTHETIC token-less users 19/20 — created rather than nulling real users' tokens), F=291 (null user1Content). Express bounced to pick up the new route (tsx has no watch mode, established pattern).",
        "Negative paths, each independently DB-confirmed to leave the row unresolved/resolvedBy=''/resolvedAt=null afterward: keep='banana' -> 400 'keep must be...'; E(290) -> 422 'No Notion access token available...'; B(287, divider) -> 422 \"Block type 'divider' does not support text content...\" AND blocks.retrieve confirmed the real divider's last_edited_time unchanged (11:16 creation) — the failed attempt never touched Notion; C(288, garbage id + keep) -> 502 with Notion's validation error surfaced; F(291, null content) -> 422 'Kept side has no captured content...'.",
        "Legacy-contract proof (not just assertion): D(289) has a garbage blockId that demonstrably 502s when keep is present (row C, same server, seconds earlier) — PATCHed WITHOUT keep -> HTTP 200, row resolved. Only possible if omitting keep truly makes zero Notion calls. conflict-004's contract is byte-identical: same validation order, same status codes, same response shape.",
        "Success path (the feature's own steps 2-4, literally): PATCH /conflicts/286/resolve {resolvedBy, keep:'user1'} -> HTTP 200, row resolved. Then read the block back DIRECTLY from the Notion API (blocks.retrieve via a separate throwaway script, parsed with the newly-exported extractBlockText): content === 'conflict-006 KEPT user1-side content' (the kept side) and !== the user2 side; the block's last_edited_time advanced to the resolve minute (11:21). Independent DB query confirmed status='resolved', resolvedBy='conflict-006-verifier', resolvedAt set — identical to conflict-004's own checks. GET /conflicts immediately after: 286 absent, the still-unresolved seeds (287/288/290) present.",
        "Regression + hygiene: GET /auth/ still 302 to api.notion.com on the same running process; grep of the Express server log for secret_/ntn_/bearer -> 0 matches (token never logged); npx tsc --noEmit clean after every change.",
        "Cleanup verified 2026-07-03: deleted exactly the 6 seeded Conflict rows (286-291) and 2 synthetic Users by id/email; trashed both scratch Notion blocks via blocks.delete; DB confirmed restored to the exact baseline (96 conflicts: 90 unresolved / 6 resolved; 2 users). All 6 throwaway scripts (server/scripts/_conflict006*.ts) deleted — only runSync.ts/runDetectConflicts.ts remain.",
        "Baseline check: initially verified against the live servers only (auth/ -> 302, / -> 200, /Dashboard -> 200) because the maintainer's own Next dev server held port 3000 (found running at session start, left untouched — dash-002 precedent); the maintainer's Express terminal process HAD to be bounced once to load the new route. UPDATE (same session, post-review): after the maintainer approved the diff and both dev servers were stopped (ports freed), the full ./init.sh cycle was run — Postgres reachable, /auth/ -> 302 PASS, / -> 200 PASS, 'Baseline smoke check PASSED', clean teardown (ports 3000/3001 confirmed free afterward). No gap remains.",
        "Maintainer review outcome 2026-07-03 (same session): diff approved as-is (explicit 'yes commit and push conflict-006'), committed and pushed. The flagged judgment calls (422/502 vocabulary, re-resolve overwrites the real block again) were not objected to and stand as implemented."
      ],
      "notes": "Passing. Depends on conflict-004 and conflict-005 (both passing). The original SCOPE NOTE is resolved as predicted: the PATCH body now carries an explicit keep: 'user1'|'user2' field; resolvedBy stays free-text and is no longer the only record of intent in the request (though still the only durable one — 'which side was kept' is deliberately NOT persisted to a column; add one later if an audit trail is wanted). Judgment call flagged for review: this route now uses 422 (well-formed request that can't be satisfied: no token, null content, non-text block type) and 502 (upstream Notion failure) — the first use of either code in this repo (everything else is 400/404/500); collapsing them to 400/500 is a one-line change if strict minimalism is preferred. Behavior change with real external consequences, called out rather than silently inherited: conflict-004's idempotent re-resolve (200, not 409, on an already-resolved conflict) is preserved, but with keep present a re-resolve OVERWRITES THE REAL NOTION BLOCK AGAIN (arguably correct 'changed my mind' semantics; harmless when re-sending the same content). MVP limitations, documented not fixed: write-back is a single plain-text run — per-segment formatting (bold/links/mentions) was already lost at capture time (extractBlockText joins plain_text), this is just where that loss becomes visible in the real workspace; Notion's ~2000-char-per-rich-text-item limit will surface as a 502 for very long content (not chunked); sibling fields inside the block's type object (to_do.checked, code.language, callout.icon) are omitted from the update body, relying on Notion's partial-update semantics to preserve them (spot-checked only for paragraph); no concurrency handling if two people resolve the same conflict simultaneously. The dashboard's Keep buttons now do a real Notion write on every click — the 93 unresolved leftover rows from conflict-005 mostly point at real blocks in the real workspace, so clicking Keep on them will now genuinely rewrite those blocks' content (with plain-text fidelity loss); worth deciding on those rows' cleanup (still pending from conflict-005) with that new weight in mind."
    },
    {
      "id": "conflict-007",
      "priority": 16,
      "area": "conflict",
      "title": "Change-based conflict detection using Notion edit metadata",
      "user_visible_behavior": "A conflict is only recorded when a block's content actually changed between syncs, attributed to the real Notion editor, and the dashboard shows the previous version next to the new version as captured. Blocks that were merely synced by multiple connections but never edited no longer appear as conflicts.",
      "status": "in_progress",
      "verification": [
        "Add notionLastEditedTime and notionLastEditedBy (the Notion editor's user id) to the Snapshot model via an additive migration, and capture both from the blocks.children.list response during sync (the block objects already carry last_edited_time/last_edited_by; no extra API call).",
        "Rewrite detectConflicts() to be change-based: for each block, compare consecutive snapshots' content; when the content changed, create a Conflict whose user1/user1Content are the previous snapshot's editor and content and whose user2/user2Content are the new snapshot's editor and content (editor mapped from Notion user id to a Concord User via User.notionId).",
        "Live test A (real change): sync, edit a disposable test block in Notion, sync again, run detection — verify exactly one Conflict row is created showing the pre-edit content vs the post-edit content with the real editor attributed.",
        "Live test B (no change): run two back-to-back syncs with no edits in between (with both connected users having sync history on the same blocks) — verify detection creates ZERO new conflicts for unchanged blocks (the presence-based false-positive class is gone).",
        "Open http://localhost:3000/Dashboard and verify the conflict from test A displays the previous and new content side by side as captured."
      ],
      "evidence": [
        "IMPLEMENTED (schema migration 20260703124406_add_notion_edit_metadata; sync.ts captures notionLastEditedTime/notionLastEditedBy; conflict.ts change-based detectConflicts(); conflicts.ts persists resolvedContent). Synthetic Phase 1 cases (M/F/N/D/T/L) all pass and tsc --noEmit is clean.",
        "REAL-DATA BUG FOUND then FIXED (why this is in_progress, not passing): running detection against the maintainer's live workspace produced 66 false-positive conflicts, all with user1Content=null. Root cause: legacy pre-conflict-005 snapshots store content=null (extractBlockText did not exist yet; it now stores \"\" for empty blocks), so a block whose only history was null/\"\" -> text was flagged as a null->text 'change'. That is block AUTHORING, not an edit of an existing version, and has no previous version to show side by side. Fix: detection now treats only non-empty snapshots as real 'versions' (candidate groupBy excludes null/\"\"; the per-block scan filters them, which also collapses a transient empty read A->\"\"->B to a single A->B change). After the fix, re-running detection on the same authored-content page creates 0 conflicts (correct); the 66 junk rows were deleted; DB back to 6 resolved.",
        "STILL TO VERIFY before passing (identified with the advisor): the feature's core promise — a REAL human edit of EXISTING (non-empty) block text in the Notion UI, attributed to a real Concord user — has never been exercised live. The scratch test was bot-attributed (fallback path); mapped attribution is proven only by synthetic case M + a query showing 2/2 real notionLastEditedBy ids match User.notionId. Needs: maintainer edits an existing block's text as one of their accounts -> sync -> detect -> exactly 1 conflict, correct prev->next, their name attributed.",
        "OPEN DESIGN QUESTIONS for the maintainer (block finalizing): (1) detection is currently MANUAL (runDetectConflicts.ts, un-wired from the 60s poller per sync-002) — this is why edits do not appear on the dashboard until detection is run; decide whether to wire it to the poller. (2) 'any content change' vs 'only when two DIFFERENT users edited the same block' — the approved design says any change (same editor counts), but confirm against the real-use expectation."
      ],
      "notes": "Motivated by the maintainer's Session 015 question: unchanged blocks were showing as conflicts. Root cause is conflict-001's documented MVP shortcut — detection is presence-based (any block with Snapshot rows from 2+ distinct userIds is flagged, where userId records who SYNCED, not who edited), so the maintainer's two Notion connections made every mutually-synced block a 'conflict' regardless of content. This feature replaces that with change-based detection. DESIGN CONFIRMED by the maintainer 2026-07-03 ('yes conflict-007 design is good'), including: a content change by ANY editor creates a conflict, even if the previous and new editor are the same person — trivially restrictable to distinct-editors-only later if wanted. The 90 unresolved presence-based false positives were DELETED 2026-07-03 with explicit maintainer approval (same message), closing the cleanup item open since conflict-005 — deleteMany({ status: 'unresolved' }) removed exactly 90 rows; the 6 resolved rows (real maintainer dashboard interactions) were kept; DB confirmed at 6 total Conflict rows after. NOTE for the implementing session: do NOT run `npm run detect-conflicts` before this feature lands — the current presence-based logic would just recreate the false-positive flood, and since conflict-006 the dashboard's Keep buttons perform real Notion writes on those rows. Depends on sync-001 (snapshot pipeline) and touches conflict-001's detectConflicts(); conflict-006's write-back and the resolve flow are unaffected (they consume Conflict rows, not the detection logic). Notion's last_edited_by may be a bot/integration id for API-driven edits and last_edited_time has minute granularity — handle unmappable editors by skipping or falling back to the syncing user, documented at implementation time."
    },
    {
      "id": "sync-005",
      "priority": 17,
      "area": "sync",
      "title": "Notion webhook receiver triggers sync + detection on real edits",
      "user_visible_behavior": "When someone edits a monitored Notion page, the change is captured and any resulting conflict appears on the dashboard within seconds — driven by Notion webhook events — instead of waiting for the 60-second poll or clicking 'Sync Now'. The 60-second poll is retained as a fallback reconciliation sweep so a missed webhook delivery still gets caught.",
      "status": "passing",
      "verification": [
        "Add an inbound POST /webhooks/notion route on the Express server (mounted in server/server.js alongside /auth, /sync, /conflicts). It must read the RAW request body (needed for signature verification) before JSON parsing.",
        "Complete Notion's one-time endpoint verification handshake: on the initial subscription request Notion sends a verification_token; capture it (store in .env / config, never commit or log it) so the subscription can be activated in the integration settings.",
        "Verify every subsequent event's X-Notion-Signature (HMAC-SHA256 over the raw body keyed by the verification token) and reject/log-and-drop any request that fails; never act on an unverified payload. The signing secret and token stay in .env and out of all logs (per AGENTS.md).",
        "Acknowledge fast: respond 200 immediately so Notion does not time out and retry, then do the sync + detect work off the response path (or complete it quickly if well under Notion's timeout).",
        "On a relevant event (e.g. page.content_updated / block-level change) for a page Concord already tracks, run the sync-001 snapshot routine for the affected page (page-scoped if practical, else syncWorkspaceForUser) and then run detectConflicts() — so a genuine content edit surfaces as a Conflict with no manual action.",
        "LIVE TEST (the feature's own verification): edit an existing (non-empty) block's text in the Notion UI on a monitored page and confirm — with no 'Sync Now' click and without waiting for the 60s poll — that a snapshot is written and (if content actually changed) exactly one Conflict appears on the dashboard within seconds, attributed to the editor. This is also the live human-edit path that conflict-007 still needs proven.",
        "Idempotency: Notion may retry or double-deliver; confirm duplicate deliveries do not create duplicate Conflicts (conflict-007's sourceSnapshotId @unique dedup already covers this, but verify end-to-end).",
        "Backstop unchanged: confirm sync-002's 60-second poll still runs and would catch a page whose webhook was missed (webhook is the fast path, poll is reconciliation — duplicate work is harmless thanks to dedup)."
      ],
      "evidence": [
        "Added server/api/webhooks.ts (mirrors api/sync.ts/api/conflicts.ts): POST /webhooks/notion mounted in server.js as app.use('/webhooks', webhooks). Order of logic: (1) if the parsed body carries { verification_token } it is Notion's one-time handshake — persist the token to a git-ignored file (server/.notion-webhook-token, mode 0600) and ack 200, logging only the file PATH and an instruction, never the token value; (2) otherwise verify X-Notion-Signature = 'sha256=' + HMAC-SHA256(rawBody) keyed by process.env.NOTION_WEBHOOK_TOKEN, compared with crypto.timingSafeEqual after a length guard — missing token / missing rawBody / mismatch each log a distinct reason and 401 (log-and-drop, never act on an unverified payload); (3) on success ack 200 FIRST, then do the work off the response path via setImmediate so Notion (retries up to 8x over ~24h on non-2xx/timeout) never times out. Signature/handshake protocol confirmed against Notion's live docs (developers.notion.com/reference/webhooks + /webhooks-events-delivery): verification_token field, X-Notion-Signature: sha256=<hex>, HMAC over the raw body keyed by the token; delivered event has top-level type, entity:{id,type}, attempt_number.",
        "server/server.js: replaced app.use(express.json()) with app.use(express.json({ verify: (req,_res,buf) => { req.rawBody = buf; } })) so the exact signed bytes are available for HMAC while every existing route's parsed req.body is unchanged. No CORS change — Notion posts server-to-server (no browser/preflight). No new dependency (crypto/fs/url are Node built-ins). No schema change.",
        "server/lib/sync.ts: extracted a behavior-preserving snapshotPage(notion,user,page) helper (the exact per-page upsert+block-snapshot body syncWorkspaceForUser already ran, including the per-page try/catch and the pages-counted-after-upsert / snapshots-counted-in-loop semantics) and added exported syncPageForUser(user,notionPageId) for the webhook fast path (notion.pages.retrieve for one page, then snapshotPage). syncWorkspaceForUser now loops snapshotPage — verified unchanged behavior. Chose page-scoped sync because a full syncWorkspaceForUser is ~15-21s (sync-002/004 evidence) and would break the 'within seconds' promise; a full-workspace fallback still runs when an event's entity.id is not a tracked page (guards a dashed/undashed id mismatch). .gitignore now ignores .notion-webhook-token.",
        "Route logic verified in isolation before any live traffic (throwaway harness on an ephemeral port, no DB writes, deleted after): handshake -> 200 + token written to the git-ignored file; missing signature -> 401; wrong signature -> 401; valid HMAC -> 200. 5/5. npx tsc --noEmit (root, covers server/*.ts): clean after every change.",
        "LIVE handshake completed 2026-07-04: local dev needs a PUBLIC URL for Notion to reach the endpoint. loca.lt was tried first and proved unusable (0/10 requests reached the server — 408/502; the maintainer's first verify attempt was silently lost this way). Switched to a cloudflared quick tunnel (downloaded to ~/.local/bin/cloudflared; no signup) which probed 8/8 reachable through plain POSTs. Created the Notion webhook subscription pointed at <tunnel>/webhooks/notion; Notion POSTed the verification_token, the handler wrote it to server/.notion-webhook-token (log line: '[webhook] verification token received and written to ...' — value NOT logged), it was pasted into Notion's activation field, and Notion now reports the subscription 'currently active'. Stored as NOTION_WEBHOOK_TOKEN in .env (git-ignored; confirmed via git check-ignore).",
        "Signature verification confirmed live end-to-end after loading NOTION_WEBHOOK_TOKEN and restarting Express: a correctly-signed event sent THROUGH the public tunnel -> HTTP 200 {ok:true}; the same event unsigned -> HTTP 401. HMAC gate is active on the real deployed path, not just the isolated harness.",
        "LIVE TEST — the feature's own core, and the real-human-edit path conflict-007 still needed: the maintainer edited existing block text in the Notion UI on tracked page id 135 (notionPageId 3817f940-ff25-80c4-9afe-c09a634cba7e), with NO 'Sync Now' click and without waiting for the 60s poll. The server log shows the full auto-driven chain within seconds: '[webhook] inbound POST (type=page.content_updated, signature present)' -> 'signature verified -> acking 200, processing off the response path' -> 'handling event type=page.content_updated entity.type=page entity.id=3817f940-...' -> 'using connected user id=12' -> 'page is tracked (local page id=135) -> page-scoped sync' -> 'sync done: 1 page(s), 91 snapshot(s). Running detection...' -> 'DONE ... conflict(s) created'. The maintainer confirmed the conflict(s) on the dashboard with no manual action.",
        "Change-based correctness / no over-detection (checked against the DB, not just log counts): Notion delivered two page.content_updated events for the edit session; the runs reported 1 then 2 conflicts created. A read-only DB inspection confirmed these are three DISTINCT real edits, not duplicates — Conflict ids 364/365/366, each with a distinct sourceSnapshotId (83968/84155/84158) and genuinely differing prev(user1Content)->new(user2Content) text on the block. The count matched the number of blocks actually changed between deliveries. A re-delivery of the same change is suppressed by the Conflict.sourceSnapshotId @unique + existingSources dedup in detectConflicts() (conflict-007), so idempotency holds end-to-end.",
        "Backstop unchanged: the 60s poll (sync-002) was left running throughout ('Sync polling started: every 60000ms.' in the boot log; 'Sync poll complete ...' lines interleaved with the webhook activity) — webhook is the fast path, poll is reconciliation, duplicate work harmless thanks to dedup.",
        "Baseline note (honest scope): the full ./init.sh baseline was NOT re-run end-to-end this session because the maintainer's Next dev (3000) and the live webhook Express+tunnel were in active use; the Express boot was confirmed healthy on the same code (server up on 3001, /auth/ path and existing routes unaffected by the new express.json({verify}) middleware) and npx tsc --noEmit is clean. Re-run the standard ./init.sh baseline after stopping the session's servers for a from-clean confirmation."
      ],
      "notes": "Passing — proven live end-to-end 2026-07-04 (real Notion edit -> webhook -> page-scoped sync -> change-based detection -> conflict on the dashboard, no manual trigger). This live run also closes conflict-007's one outstanding gap (a real human edit of existing block text, attributed to a real Concord user, exercised live). RUNTIME SETUP (not committed, session-scoped): the Express server, the public tunnel, and NOTION_WEBHOOK_TOKEN in .env are required for the webhook to fire but are not part of the repo — a fresh session must re-establish a public tunnel and (because trycloudflare/loca.lt URLs are ephemeral per start) re-point the Notion subscription's Webhook URL, unless deploy-002 provides a stable hosted URL. TUNNEL LESSON for next session: loca.lt was unreliable here (0/10); a cloudflared quick tunnel (no signup) was reliable (8/8). Replaces MANUAL/polled triggering as the PRIMARY path while keeping sync-002 (60s poll) as a fallback backstop — maintainer decision 2026-07-03 ('keep poll as fallback'). Depends on sync-001 (snapshot routine), sync-002 (backstop poll), and conflict-007 (change-based detection + sourceSnapshotId dedup + real-editor attribution). This directly resolves conflict-007's open 'manual vs auto detection' question by making detection event-driven. Local dev needs a PUBLIC URL for Notion to reach the endpoint (a tunnel such as ngrok, or the deployed instance from deploy-002); note this in the implementing session. Notion webhook events reference the changed page/block ids but do NOT include an access token or who-edited attribution beyond ids — attribution still comes from the subsequent sync's last_edited_by capture (conflict-007), so the flow is: webhook event -> page-scoped sync (captures last_edited_by) -> detectConflicts(). Security: verification_token and any signing secret live in .env (git-ignored) and must never be logged or committed. Inserted at priority 17 on maintainer instruction; team-001..deploy-002 bumped +1 (same insertion pattern used for conflict-007)."
    },
    {
      "id": "dash-005",
      "priority": 18,
      "area": "dashboard",
      "title": "Live dashboard: new conflicts appear in real time without a refresh or button click",
      "user_visible_behavior": "With the dashboard open and untouched, a conflict produced by a Notion edit (delivered via the sync-005 webhook) appears in the list automatically within seconds — no page refresh and no 'Sync Now' / button click.",
      "status": "passing",
      "verification": [
        "Add a server->browser push channel so the dashboard updates itself when a Conflict row is created. Recommended: Server-Sent Events (SSE) — a GET /events (or /conflicts/stream) endpoint on the Express server (3001) that holds the response open and writes 'data: ...\\n\\n' frames. The existing corsOptions already allows the http://localhost:3000 origin the EventSource GET needs.",
        "Emit an event whenever detection creates a conflict: have the sync-005 webhook path (and/or detectConflicts) signal the SSE endpoint in-process (e.g. a shared Node EventEmitter) when conflictsCreated > 0, carrying at least the new conflict id(s)/pageId.",
        "Add a 'use client' component mounted on app/Dashboard/page.tsx that opens an EventSource to the endpoint and, on a message, calls router.refresh() (the same revalidation SyncNowButton/ResolveConflictButtons already use) so the server component re-queries Prisma and re-renders with the new conflict — without a full page reload.",
        "LIVE TEST: open http://localhost:3000/Dashboard and do NOT touch it. Edit an existing block's text in the Notion UI on a monitored page. Confirm the resulting conflict appears in the list within seconds with no refresh and no button click.",
        "No-op case: with the dashboard open, run a sync that detects zero new conflicts (no real content change) and confirm the list does not change and there is no spurious update/flicker.",
        "Resilience: confirm the EventSource reconnects on its own if the connection drops (bounce the Express server, or the tunnel blips) and resumes receiving updates without a manual refresh.",
        "Backstop unchanged: sync-002's 60s poll and the sync-005 webhook still function; this feature only adds the browser-facing live update, it does not replace them."
      ],
      "evidence": [
        "SSE chosen over WebSocket, reconfirmed with the maintainer during implementation ('if WebSocket need add it' -> presented the one-directional trade-off, maintainer selected SSE): the channel is strictly server->client, so WebSocket's full-duplex + new `ws` dependency + manual reconnect buy nothing; EventSource is a browser built-in and auto-reconnects.",
        "Added server/lib/conflictEvents.ts: a module-singleton Node EventEmitter (the in-process signal channel — Node caches modules per process, so the webhook handler that creates conflicts and the SSE endpoint that serves browser clients share one instance; no broker needed because the only in-process conflict-creating path is the sync-005 webhook handler, co-located with the SSE clients). setMaxListeners(0) since one listener is added per open dashboard tab. Exports emitConflictsCreated({ pageId, count }).",
        "server/api/webhooks.ts: after the existing detectConflicts() call in handleEvent, added `if (detection.conflictsCreated > 0) emitConflictsCreated({ pageId: tracked?.id ?? null, count: detection.conflictsCreated })`. Emitting HERE (not inside detectConflicts()) keeps conflict-007's detection function and its return type untouched (in-scope-only), and the webhook is the sole in-process path that both creates conflicts and shares the process with SSE clients (the manual runDetectConflicts.ts is a separate process — its emit would reach no listeners). Emitting only when >0 gives the no-op case (a zero-change run pushes nothing) for free.",
        "Added server/api/events.ts (GET /events, mirrors the one-router-per-file api/*.ts convention; mounted in server/server.js as app.use('/events', events)): writes text/event-stream headers, an initial 'retry: 3000' + ': connected' frame, subscribes onConflict -> `data: <json>` to conflictEvents, a 25s ': ping' heartbeat, and on req 'close' BOTH clearInterval(heartbeat) AND conflictEvents.off(...) then res.end() (leaking either would accumulate one per reconnect, and reconnects are frequent by design). No CORS change (global cors already allows the localhost:3000 EventSource origin — confirmed in the response headers below); the global express.json({verify}) is a no-op on this bodyless GET.",
        "Added app/components/conflict-live-updates/ConflictLiveUpdates.tsx ('use client', renders null): opens an EventSource to http://localhost:3001/events in useEffect and, on each message, startTransition(() => router.refresh()) — the exact revalidation path SyncNowButton/ResolveConflictButtons already use, so the Server Component re-queries Prisma and swaps in the new conflict with no full reload. No onerror handler needed (EventSource auto-reconnects; server also sends retry:3000). Cleanup closes the source on unmount. Mounted once inside <main> in app/Dashboard/page.tsx.",
        "npx tsc --noEmit: clean (after implementation and again as the final gate).",
        "Verified 2026-07-04. DB baseline before any test: 9 resolved / 0 unresolved Conflict rows (max id 366). The whole push channel is localhost:3000<->localhost:3001 and never touches the sync-005 cloudflared tunnel, so the in-process emit was triggered deterministically by self-delivering a signed webhook (NOTION_WEBHOOK_TOKEN is in .env) — no live Notion delivery / tunnel needed.",
        "Transport check (curl -sN -D - http://localhost:3001/events): HTTP 200, Content-Type: text/event-stream, Access-Control-Allow-Origin: http://localhost:3000 (proves CORS covers the EventSource GET), the 'retry: 3000' + ': connected' frames, stream held open until the timeout cut it. Heartbeat check: a separate 28s capture recorded exactly 1 ': ping' (25s interval).",
        "Positive push + no-op (deterministic, no tunnel — throwaway driver, deleted after): appended a scratch paragraph block to real tracked page id=135, captured it (syncPageForUser -> v1 snapshot), edited it to v2 via the Notion API (blocks.update — a real change; sets last_edited_time non-null, clearing conflict-007's historical-noise guard; fresh snapshot id clears the sourceSnapshotId dedup; content matches no resolvedContent so the anti-loop guard passes). Opened an SSE listener, then POSTed a signed page.content_updated event (X-Notion-Signature = sha256=HMAC-SHA256(rawBody, token), computed over the exact bytes) to /webhooks/notion. Result: webhook 200, server ran sync -> detect -> created exactly 1 Conflict (id 367, blockId = the scratch block, user1Content='dash-005 scratch v1...', user2Content='dash-005 scratch v2...'), and the SSE listener received exactly one frame: `data: {\"pageId\":135,\"count\":1}` (correct pageId + count). NO-OP: re-firing the signed webhook with NO intervening edit -> conflictsCreated=0 -> 0 new SSE frames (the idle dashboard would not flicker).",
        "Real browser check of the client component (puppeteer-core@25 in the session scratchpad only — not committed — driving system google-chrome-stable headless; both dev servers up via npm run dev + npm start): loaded /Dashboard, set a window.__guard sentinel, then produced a live push (edit scratch block -> signed webhook). The new conflict card appeared with the card count rising by exactly one and the sentinel STILL intact (liveUpdateWorked=true, noHardReload=true — proves router.refresh() soft-revalidation, not a full document reload). Resilience: bounced the Express server (killed :3001, restarted) — the browser's EventSource reconnected on its own (observed the /events request count rise from 1 to 3 with no manual refresh), and a subsequent signed-webhook push was delivered live (card count rose again). Zero page/console errors throughout.",
        "Cleanup verified 2026-07-04: deleted the 3 test Conflict rows (367/368/369 from the v1->v2, v3, v4 pushes), the 10 scratch-block Snapshot rows, and trashed the scratch Notion block via blocks.delete. DB confirmed restored to the exact baseline (9 resolved / 0 unresolved, max conflict id 366, 0 scratch snapshots). All four throwaway server/scripts/_dash005_*.ts and the scratchpad browser driver deleted — only runSync.ts/runDetectConflicts.ts remain in server/scripts. Token never logged (webhook handler already omits it; the driver signed in-process).",
        "./init.sh baseline smoke check re-run from a clean boot after full teardown: Postgres reachable, /auth/ -> 302 to api.notion.com, / -> 200, 'Baseline smoke check PASSED', clean teardown (ports 3000/3001 confirmed free afterward)."
      ],
      "notes": "Passing — verified live 2026-07-04 (deterministic self-delivered signed webhook -> emit -> SSE frame, plus a headless-browser run showing the new conflict card appear with no reload and the EventSource reconnecting after an Express bounce). Completes the real-time story started by sync-005: sync-005 lands the conflict in Postgres within seconds of a Notion edit, but app/Dashboard/page.tsx is a Next.js server component that today only reflects new rows on load/refresh — so the user still has to refresh to see it. This feature adds the missing server->browser push so the OPEN dashboard updates with no user action. Transport chosen: SSE (maintainer confirmed over WebSocket during implementation), one-directional server->client, zero new dependencies. Depends on sync-005 (event-driven conflict creation), conflict-007 (change-based detection), and conflict-002/conflict-005 (dashboard conflict rendering + resolve UI). Design leaning (confirm at implementation): SSE over WebSocket — updates are one-directional server->client, SSE is simpler over the existing Express+CORS setup and auto-reconnects; a client-side interval poll of GET /conflicts is an acceptable MVP fallback (also satisfies 'no refresh/button') but is periodic, not true push. Inherits the pre-existing dynamic-rendering caveat that every feature touching app/Dashboard/page.tsx carries (see conflict-002). COUPLING NOTE for whoever resolves conflict-007's open 'wire detectConflicts into the 60s poller?' question: the SSE emit lives in the webhook handler (server/api/webhooks.ts), NOT inside detectConflicts(), so if detection is ever wired into another in-process path (e.g. the sync-002 poller), that path must ALSO call emitConflictsCreated({ pageId, count }) or conflicts it creates will silently not push to open dashboards. DEPLOY-002 NOTE: app/components/conflict-live-updates/ConflictLiveUpdates.tsx adds a new hardcoded 'http://localhost:3001/events' reference — joins the running deploy-002 inventory (login.jsx, runSync.ts, auth.ts redirectUri, SyncNowButton's SYNC_ENDPOINT, ResolveConflictButtons) that breaks under a hosted split. Inserted at priority 18 on maintainer instruction; team-001..deploy-002 bumped +1 (same insertion pattern used for conflict-007 and sync-005)."
    },
    {
      "id": "bug-001",
      "priority": 19,
      "area": "conflict",
      "type": "bug",
      "title": "Resolved conflict still appears in the dashboard after Keep-User1/User2",
      "user_visible_behavior": "After clicking 'Keep User1' or 'Keep User2', the kept version is written back to the real Notion block (verified in Notion), but the conflict card stays visible in the dashboard conflict list instead of disappearing.",
      "status": "in_progress",
      "reported": "2026-07-09",
      "expected": "Once a conflict is resolved, its card is removed from the dashboard (the list is filtered to status='unresolved' and router.refresh() re-runs that query).",
      "actual": "Notion write-back succeeds and the Conflict row is set to status='resolved', but a card for that block remains in the UI.",
      "repro": [
        "Have at least one unresolved conflict on the dashboard.",
        "Click 'Keep User1' (or 'Keep User2').",
        "Confirm the kept content lands in the Notion block (write-back works).",
        "Observe the dashboard: a conflict card for the same block is still shown."
      ],
      "suspected_cause": [
        "The successful write-back edits the Notion block, which fires the webhook (sync-005) -> sync -> detectConflicts, and change-based detection (conflict-007, still in_progress) likely records the write-back itself as a NEW unresolved Conflict row. The resolvedContent anti-loop guard added in conflict-006/007 may not be matching, so a fresh card reappears.",
        "Alternatively the live-updates stream (dash-005) re-emits/keeps the conflict client-side even after router.refresh() clears the server-rendered card.",
        "Confirm which by checking the DB after resolve: if a new Conflict row (new id, status='unresolved') exists for the same blockId, it's the re-detection path; if only the original row exists (now status='resolved') yet the card persists, it's the live-updates/refresh path."
      ],
      "verification": [
        "ORDERING (fix + cleanup are inert until botId is populated): deploy -> a connected user re-runs Notion OAuth (populates User.botId) -> run cleanup -> verify. Before any re-auth, every User.botId is null, so the detection guard matches nothing and cleanup no-ops ('No User.botId values found').",
        "Apply migration when DB reachable: `npx prisma migrate deploy` (or migrate dev).",
        "LINCHPIN assertion (proves the fix's core assumption): after a real Keep-User1 write-back, read the newly-created Snapshot for that block and assert snapshot.notionLastEditedBy === the acting user's User.botId. The whole fix depends on users.me() botDetails.id equalling the last_edited_by Notion stamps on the write-back; this is the one check that confirms it directly.",
        "Confirm the IDENTITY guard (not the fallback) did the work: the detection run must log '[detect] suppressed write-back landing on block <id> (edited by bot <id>)'. A '0 conflicts' result alone is NOT sufficient — the resolvedContent/time fallback can also yield 0 in a clean case and would mask a broken identity guard.",
        "Real e2e (Keep User1 specifically): OAuth -> create a 2-user edit conflict -> Keep User1 -> confirm kept text lands in Notion -> confirm the next webhook re-sync logs the suppression line above AND '[webhook] DONE ... 0 conflict(s) created', with no new unresolved row; card stays gone.",
        "Offline mechanism check (secondary, NOT primary evidence — it is circular on the id equality since you seed both sides): seed User.botId='bot-x' + a snapshot A' with notionLastEditedBy='bot-x' after a resolved conflict; detectConflicts() -> 0 created. Proves the guard fires when ids match; does not prove real Notion produces that match (the linchpin assertion does).",
        "Run `cd server && npm run cleanup-bot-conflicts` once (after re-auth) to clear pre-existing stuck rows.",
        "./init.sh baseline smoke check still passes."
      ],
      "evidence": [
        "Offline checks passed: `npx prisma generate` regenerated the client with User.botId (56 refs in app/generated/prisma). conflict.ts + auth.ts import-load cleanly via tsx. Cleanup script transpiles via esbuild.",
        "PENDING (DB unreachable from this env — Supabase aws-0-ap-southeast-2 TCP:5432 refused even with sandbox off): apply migration, run offline reproduce->fix, real Notion Keep-User1 e2e, ./init.sh baseline."
      ],
      "notes": "Fix implemented and offline-verified; behavioral verification (above) is PENDING because the Supabase DB is unreachable from the dev environment. Handoff ordering is load-bearing: the fix and cleanup do nothing until a connected user re-authenticates (User.botId is null on all existing rows until re-OAuth). Caveat: collecting all botIds only protects write-backs whose acting token belongs to a re-authed user; a write-back driven by a still-null-botId user's token falls through to the fragile resolvedContent/time fallback. Per AGENTS.md, do not commit until the maintainer reviews. Related: conflict-006 (write-back), conflict-007 (change-based detection).",
      "root_cause_confirmed": "Confirmed. The reappearing card is a NEW unresolved Conflict row, not the original. Reproduces on 'Keep User1' (restoring the non-current side): the write-back genuinely edits the block -> webhook -> detectConflicts creates a fresh row. The resolvedContent/time anti-loop guard (conflict.ts) is brittle: exact content equality breaks on Notion's write-back round-trip reformatting, and resolvedAt>=prev.createdAt compares two clocks (app new Date() vs Postgres now()). 'Keep User2' is a content no-op already caught by the sourceSnapshotId dedup. Live-updates (dash-005) holds no client state; it only surfaces the new DB row faster.",
      "fix_implemented": "Identity-based anti-loop. The resolve write-back is an API edit, so Notion stamps the block's last_edited_by with the integration's bot user id. Persist that id (User.botId, from botDetails.id at OAuth) and, in detectConflicts, skip any change whose new-side snapshot.notionLastEditedBy is a known bot id. Exact identity match — immune to content reformatting and clock skew. The old resolvedContent/time guard is kept as a fallback.",
      "changes": [
        "prisma/schema.prisma: add User.botId String?",
        "prisma/migrations/20260709120000_add_user_bot_id/migration.sql: ALTER TABLE \"User\" ADD COLUMN \"botId\" TEXT (hand-authored; NOT yet applied — DB unreachable from the dev env).",
        "server/api/auth.ts: persist botId: botDetails.id in the OAuth upsert (create + update).",
        "server/lib/conflict.ts: build botIds set from User.botId; primary guard `if (next.notionLastEditedBy && botIds.has(...)) continue;` before conflict.create.",
        "server/scripts/resolveBotWritebackConflicts.ts (+ npm run cleanup-bot-conflicts): one-off, marks existing bot-authored stuck rows status=resolved, resolvedBy='system:bug-001' (non-destructive)."
      ]
    },
    {
      "id": "team-001",
      "priority": 20,
      "area": "team",
      "title": "GET /team lists workspace members",
      "user_visible_behavior": "The app can list all users connected to the same workspace as one team.",
      "status": "not_started",
      "verification": [
        "Seed two User rows sharing the same workspaceId.",
        "curl http://localhost:3001/team for that workspace.",
        "Assert both members are returned with name, email, and avatar; users from other workspaces are excluded."
      ],
      "evidence": [],
      "notes": "Phase 6 workspace concept: members are Users grouped by workspaceId (already on the User model)."
    },
    {
      "id": "team-002",
      "priority": 21,
      "area": "team",
      "title": "Email notification when a new conflict is detected (nodemailer)",
      "user_visible_behavior": "When a new conflict is detected, the involved team members receive an email notification.",
      "status": "not_started",
      "verification": [
        "Configure nodemailer with test SMTP credentials (e.g. an Ethereal/dev transport) via .env.",
        "Trigger conflict detection so a new Conflict row is created.",
        "Verify a notification email is sent to the involved users (capture the message id / preview URL from the transport)."
      ],
      "evidence": [],
      "notes": "Depends on conflict-001. Adds `nodemailer`. SMTP creds are secrets — keep them in .env, never log or commit them."
    },
    {
      "id": "team-003",
      "priority": 22,
      "area": "team",
      "title": "Team members page (avatar, name, conflict count)",
      "user_visible_behavior": "A team page shows each member's avatar, name, and how many conflicts they are involved in.",
      "status": "not_started",
      "verification": [
        "Seed workspace members and Conflict rows involving them.",
        "Open the team members page in the frontend.",
        "Verify each member shows avatar, name, and a conflict count (conflicts where they are user1 or user2)."
      ],
      "evidence": [],
      "notes": "Depends on team-001 and conflict-001."
    },
    {
      "id": "team-004",
      "priority": 23,
      "area": "team",
      "title": "Filter conflicts by member and new-conflict notification badge",
      "user_visible_behavior": "The user can filter the conflicts list by team member and sees a badge on the dashboard when a new conflict arrives.",
      "status": "not_started",
      "verification": [
        "Open the dashboard with conflicts involving multiple members.",
        "Filter by one member and verify only conflicts involving that member remain.",
        "Create a new conflict (or simulate one) and verify a notification badge appears on the dashboard."
      ],
      "evidence": [],
      "notes": "Depends on conflict-002/conflict-005 and team-001."
    },
    {
      "id": "polish-001",
      "priority": 24,
      "area": "polish",
      "title": "Error, loading, and empty states + mobile responsiveness",
      "user_visible_behavior": "The app handles errors gracefully, shows loading indicators while fetching, shows friendly empty states, and is usable on mobile.",
      "status": "not_started",
      "verification": [
        "Load the dashboard with no conflicts and verify an empty state (e.g. 'No conflicts detected 🎉').",
        "Simulate a failing backend call and verify an error state renders instead of a crash.",
        "Verify loading indicators appear during data fetches.",
        "Check the dashboard at a mobile viewport width (~375px) and confirm the layout stays usable."
      ],
      "evidence": [],
      "notes": "Phase 7 polish. Broad by nature — verify per screen rather than a single assertion."
    },
    {
      "id": "deploy-001",
      "priority": 25,
      "area": "deploy",
      "title": "Seed demo data for showcase",
      "user_visible_behavior": "A seed script populates realistic demo users, pages, snapshots, and conflicts so the dashboard looks populated for a demo.",
      "status": "not_started",
      "verification": [
        "Run the seed script against a clean database.",
        "Verify User, Page, Snapshot, and Conflict rows (mix of resolved/unresolved) are created.",
        "Open http://localhost:3000/Dashboard and confirm the stat cards, chart, and lists render with the seeded data."
      ],
      "evidence": [],
      "notes": "Phase 7. Keep the seed idempotent/reset-safe so it can be re-run for demos."
    },
    {
      "id": "deploy-002",
      "priority": 26,
      "area": "deploy",
      "title": "Deploy to Supabase + Render + Vercel",
      "user_visible_behavior": "The app is reachable at a public URL: frontend on Vercel, backend on Render, database on Supabase.",
      "status": "not_started",
      "verification": [
        "Provision the Postgres database on Supabase and run migrations against it.",
        "Deploy the Express server to Render with the required env vars set (no secrets committed).",
        "Deploy the Next.js frontend to Vercel pointed at the deployed backend.",
        "Complete a live Notion OAuth login end to end and confirm the dashboard loads with real data."
      ],
      "evidence": [],
      "notes": "Phase 7. Update the Notion OAuth redirect_uri to the deployed callback URL. All secrets via platform env vars, never committed."
    },
    {
      "id": "team-005",
      "priority": 27,
      "area": "team",
      "title": "Connected members page — shown only after Notion OAuth login",
      "user_visible_behavior": "After a user logs in via Notion OAuth, they can open a members page that lists the members connected to their workspace (name, avatar, email). The page is gated behind login — an unauthenticated visitor does not see the members list.",
      "status": "not_started",
      "verification": [
        "Log in via Notion OAuth (auth-001/auth-002) so a session/login state exists for the connected user.",
        "Open the connected-members page in the frontend and verify it lists the User rows sharing the logged-in user's workspaceId, each with name, avatar, and email.",
        "Confirm the page is NOT accessible / shows a login prompt when there is no logged-in user (the gate actually blocks unauthenticated access).",
        "Confirm members from other workspaces are excluded."
      ],
      "evidence": [],
      "notes": "Requested by the maintainer (Session 018): 'only I need connected member page when I login by Notion OAuth'. OVERLAP to reconcile before building: team-001 (GET /team lists workspace members) and team-003 (team members page with avatar/name/conflict count) already cover most of the data/UI — this feature's distinct requirement is the LOGIN GATE (only visible when authenticated) and scoping to CONNECTED members. Consider building on team-001's endpoint rather than duplicating. KEY CONSTRAINT: there is currently NO auth/session system wired (server/api/auth.ts upserts the User and redirects, but sets no cookie/session; jsonwebtoken + cookie-parser are installed yet unused — confirmed across conflict-004/conflict-005 notes). So this feature must ADD the login-state gate (session cookie or equivalent) as part of its scope, or it cannot enforce 'only when I login'. 'Connected members' == User rows (auth.ts only creates a User via OAuth), filtered by the logged-in user's workspaceId. Priority 26 is a non-disruptive append; reprioritize relative to team-001/003 as desired. Depends on auth-002 (login) and the User model."
    },
    {
      "id": "team-006",
      "priority": 28,
      "area": "team",
      "title": "Decide connection model: one 'main' connected user vs. every member connecting via OAuth",
      "user_visible_behavior": "A documented, confirmed decision on whether Concord requires each teammate to connect via Notion OAuth, or can run on a single 'main' connected user — with the attribution behaviour of each option made explicit so the team page and conflict attribution are not misleading.",
      "status": "not_started",
      "verification": [
        "Confirm the finding below against the live app: with only ONE connected user, run the full pipeline (webhook edit -> sync -> detect -> conflict on dashboard -> resolve/write-back) and verify it works end to end.",
        "Confirm attribution behaviour: an edit made by a Notion member who has NOT connected to Concord is attributed (via last_edited_by -> notionId mapping) — verify whether it resolves to the real editor or falls back to the syncing user.",
        "Record the maintainer's decision (single-main-user is sufficient vs. require each member to connect) and, if attribution-by-real-name matters, capture whether to add a Notion users.retrieve lookup so unmapped editors get named without connecting."
      ],
      "evidence": [],
      "notes": "Requested by the maintainer (Session 018): 'two users need to connect in app or one main user is enough — check that'. PRELIMINARY FINDING from code review this session (server/api/auth.ts, server/lib/sync.ts, server/lib/conflict.ts): (1) User rows are created ONLY by the OAuth callback (upsert on notionId) — no other path creates a User. (2) Sync uses a SINGLE token: syncWorkspaceForUser, the sync-002 poller, POST /sync (sync-004), and the sync-005 webhook all select `prisma.user.findFirst({ where: { accessToken: { not: null } } })` (the first connected user), and that one token reads every page the integration can access. (3) Detection is change-based (conflict-007): the editor is taken from Notion's last_edited_by and mapped to a Concord User via User.notionId by mapEditor(); if the editor has no matching User row it FALLS BACK to the syncing user's id. CONCLUSION: ONE 'main' connected user is ENOUGH for the app to FUNCTION end to end (sync -> detect -> resolve -> conflict-006 write-back, which uses the kept user's token falling back to the other participant's). The old 'two connected users cause a presence-based false-positive flood' problem (sync-002/conflict-001) is GONE post conflict-007. HOWEVER attribution degrades: if the real editor never connected via OAuth, there is no notionId->User mapping, so both sides of a conflict collapse onto the single syncing user's name. To show each teammate's REAL identity on a conflict, that teammate must connect via OAuth at least once, OR the app must add a Notion users.retrieve call to name unmapped editor ids. RECOMMENDATION: one main user is sufficient to run and demo the app; require each member to connect only if per-editor attribution by real name is a product requirement. This is an investigation/decision task, not a code feature — close it by recording the decision (and spin off a real feature if 'name unmapped editors' is chosen). Depends on conflict-007 (attribution logic) and sync-005 (live pipeline). DECISION (2026-07-04, maintainer): OPTION B chosen — keep the single-main-user connection model (members are NOT required to connect), and add a Notion users.retrieve lookup so unmapped last_edited_by ids are named with their real Notion name/avatar without OAuth. IMPLEMENTED (2026-07-04, pending live verification): added async resolveEditor() in server/lib/conflict.ts (Notion users.retrieve on an unmapped last_edited_by id -> upsert a token-less shadow User -> attribute to real name/avatar; falls back to the syncing user on no-token/bot/failure); threaded the syncing user's token+workspace from server/api/webhooks.ts and server/scripts/runDetectConflicts.ts; and added an any-connected-user write-back token fallback in server/api/conflicts.ts so a conflict between two shadow (unconnected) editors can still resolve. Static verification done: full server subtree typechecks (NodeNext strict) and the init.sh baseline smoke check passes (both servers boot, /auth redirect + frontend OK). STAYS not_started until behavioral verification on a live DB + real 2nd Notion member (integration needs the 'read user information' capability): confirm real-name attribution, two-distinct-editor display, no duplicate shadow rows on re-run, graceful bot/failure fallback, and resolve->write-back for a two-shadow-user conflict."
    }
  ]
}
